# Parity gaps — rules-python (python-fastapi ADBP)

Only GAPS listed (no/partial backing). Fully-backed families omitted: Any/type-annotations, dynamic exec/pickle/yaml/subprocess/os.system, wildcard+relative imports, bare/broad except + except-pass, print, runtime asserts, sleep-in-async, naive datetime, mutable defaults, http-timeouts, global mutable state, dynamic imports, env-read-in-config-boundary, frozen dataclass VOs, naked-string/magic-string→enum, file-line-budget, nesting-depth, ruff/pyright config + strict mode, lockfile/pinning, pydantic/domain-model shape rules, weak-assert/empty-test/behavior-assert/validator+exception-path tests.

| ADBP point | ADBP source | Backed? (family or NO/PARTIAL) | Tier | Proposed ruleId | Fail-fixture | Pass-fixture |
|---|---|---|---|---|---|---|
| FSM mandatory for any stateful entity (status field with transitions); illegal transitions must raise; FSM in `state_machines/` | enums-fsm.md | NO | T1 | py-fastapi-fsm-required-for-status-mutation | service assigns `order.status = target` with no transition-map/validate call | assignment guarded by `transition_order()` / transition-map lookup that raises on illegal |
| Enum mandatory for fixed value sets; `if x == "active"` string literal → must be enum | enums-fsm.md | PARTIAL (naked-string-alias, magic-string-comparison exist; no "fixed-set literal must be enum" rule) | T1 | py-fastapi-status-string-literal-forbidden | `if status == "pending":` string literal on a status/role/type field | comparison against `OrderStatus.PENDING` enum member |
| Enums must live in `enums/` directory | enums-fsm.md | NO | T1 | py-fastapi-enum-location | `class OrderStatus(StrEnum)` defined in `services/` | same enum defined under `enums/` |
| FSMs must live in `state_machines/`; enums in `enums/`, etc. — canonical layout | architecture.md | NO | T1 | py-fastapi-canonical-layout | transition-map dict defined in `models/order.py` | defined in `state_machines/order_fsm.py` |
| Routers: no business logic, no DB queries, no direct model imports | architecture.md | PARTIAL (import-linter contracts exist for this repo, not shipped as py rule) | T1 | py-fastapi-router-no-model-import | `routers/users.py` imports from `models`/`repositories` or calls `db.query` | router imports only schemas/services |
| Endpoints must declare explicit `response_model` | architecture.md | NO | T1 | py-fastapi-endpoint-response-model | `@router.post("/users")` with no `response_model=` | `@router.post("/users", response_model=UserOut)` |
| Endpoints must declare explicit `status_code` | architecture.md | NO | T2 | py-fastapi-endpoint-status-code | route decorator missing `status_code=` | `status_code=status.HTTP_201_CREATED` present |
| Services must not receive/import `Session` or run queries | architecture.md | NO | T1 | py-fastapi-service-no-session | `def __init__(self, db: Session)` in a service | service `__init__` takes repo Protocol only |
| Services must not manage transactions (`db.commit/flush/rollback/add`) | architecture.md | NO | T1 | py-fastapi-service-no-transaction | `db.commit()` inside a service method | commit only in repository/UoW |
| Services must raise domain exceptions, not `HTTPException` | architecture.md | NO | T1 | py-fastapi-service-no-httpexception | `raise HTTPException(...)` in `services/` | `raise UserNotFoundError(...)` |
| No module-level service singletons / module-level repo imports; wire via `Depends()` + `__init__` | architecture.md, solid | PARTIAL (global-mutable-state exists; not DI-specific) | T1 | py-fastapi-no-module-singleton | `service = UserService()` at module scope | service constructed via `Depends()` factory |
| Repositories are the only layer importing ORM models/`Session`; no business logic | architecture.md | NO | T1 | py-fastapi-repo-only-orm-import | `repositories/*` contains validation/business branch or is imported by `services` layer as `Session` passthrough | repo methods pure data access |
| Repositories must not import services/routers/workflows | tooling.md (importlinter contract) | NO | T1 | py-fastapi-repo-forbidden-upward-import | `repositories/x.py` imports `services` | no upward imports |
| Workflows may depend on services but never repositories | architecture.md | NO | T1 | py-fastapi-workflow-no-repo-import | `workflows/*` imports `repositories` | workflow imports services only |
| Separate schemas per purpose; never reuse one schema for input+output | architecture.md, patterns | NO | T2 | py-fastapi-schema-input-output-split | same Pydantic model used as request body and `response_model` | distinct `Create`/`Out` models |
| ORM models must not contain business-logic methods | architecture.md | NO | T2 | py-fastapi-model-no-business-method | non-trivial method (branches/domain calc) on a `models/` class | models declare columns only |
| Function argument count ≤ 5 | module-size.md | NO | T1 | py-fastapi-max-function-args | function with 6+ params | ≤5 params or grouped into a model |
| Function length ≤ ~30 lines | module-size.md | PARTIAL (file-line-budget exists; no per-function length) | T2 | py-fastapi-function-length | function body > 30 lines | ≤30 |
| Class length ≤ ~150 lines | module-size.md | NO | T2 | py-fastapi-class-length | class > 150 lines | ≤150 |
| Cyclomatic complexity < 10 (radon) | module-size.md, tooling | NO | T2 | py-fastapi-cyclomatic-complexity | function radon grade ≥ C | grade A/B |
| No catch-all `utils.py`/`helpers.py` (>50 lines must split; topic-specific names) | module-size.md | PARTIAL (dumping-ground-module-names exists — verify covers utils.py>50) | T2 | py-fastapi-utils-catchall | `utils.py` > 50 lines | `utils/hashing.py` topic-specific |
| Package nesting depth ≤ 3 levels | module-size.md | NO | T2 | py-fastapi-package-nesting-depth | `src/app/core/services/domain/user/service.py` | ≤3-level path |
| KISS: prefer guard clauses / early returns over deep nesting | kiss-dry-yagni.md | PARTIAL (nesting-depth-budget exists) | T2 | (covered by nesting budget — verify threshold) | — | — |
| YAGNI: delete dead code, don't comment out | kiss-dry-yagni.md | PARTIAL (ERA/eradicate in ruff config only; no shipped rule) | T2 | py-fastapi-commented-out-code | block of commented-out code | removed |
| List endpoints must be paginated (no unbounded collections) | patterns.md | NO | T2 | py-fastapi-list-endpoint-pagination | `response_model=list[X]` with no limit/pagination params | paginated response with limit/offset or cursor |
| Structured logging; log boundaries; never log secrets/PII; correct levels | patterns.md | NO | T2 | py-fastapi-structured-logging | `logging.getLogger` plain / f-string log of password/token | structlog / lazy `%s` logging, no PII |
| pydantic-settings `BaseSettings`; no hardcoded secrets/URLs/env values | patterns.md, security | PARTIAL (env-reads-in-config-boundary exists; no BaseSettings-required / hardcoded-secret literal rule) | T1 | py-fastapi-hardcoded-secret | `API_KEY = "sk-..."` / `DATABASE_URL = "postgres://user:pw@..."` literal | value read from `BaseSettings` |
| Async SQLAlchemy only (`AsyncSession`); no legacy `Query` API; `select()` 2.0 style | patterns.md | NO | T2 | py-fastapi-legacy-sqlalchemy-query | `session.query(User)` legacy API | `select(User)` |
| Alembic for migrations — no manual schema edits | patterns.md | NO | T3 | py-fastapi-alembic-migrations (labeled: no mechanical schema-edit detector) | — | — |
| `asyncio.gather`/`TaskGroup` for concurrency; no sync blocking in async without executor | patterns.md | PARTIAL (sleep-in-async + asyncio-task-tracked exist; no general sync-blocking-in-async) | T2 | py-fastapi-sync-blocking-in-async | `requests.get()` / `open()` blocking call in `async def` | `httpx`/`await`/`run_in_executor` |
| Idempotency keys on critical POST/PUT | patterns.md | NO | T3 | py-fastapi-idempotency-key (labeled: semantic, no mechanical check) | — | — |
| Passwords hashed with bcrypt/argon2 — never plaintext | security.md | NO | T1 | py-fastapi-plaintext-password | password stored/compared as plaintext str | `bcrypt`/`argon2` hash |
| Constant-time token/secret comparison (`hmac.compare_digest`) | security.md | NO | T1 | py-fastapi-nonconstant-token-compare | `if user_token == stored_token:` | `hmac.compare_digest(...)` |
| Cryptographically secure randomness (`secrets`, not `random`) for tokens | security.md | NO | T1 | py-fastapi-insecure-random-token | `random.choices(...)` for token | `secrets.token_hex(...)` |
| No `allow_origins=["*"]` CORS in production | security.md, patterns | NO | T1 | py-fastapi-cors-wildcard | `CORSMiddleware(allow_origins=["*"])` | explicit origin list |
| Path traversal: validate resolved path stays under base dir | security.md | NO | T1 | py-fastapi-path-traversal | `open(f"/data/{filename}")` from request input | `is_relative_to(BASE_DIR)` check |
| IDOR: ownership check on resource-by-id endpoints | security.md | NO | T3 | py-fastapi-idor-ownership-check (labeled: ownership semantics not mechanically inferable) | — | — |
| Error handling: don't return raw exception str to client (info disclosure) | security.md | NO | T1 | py-fastapi-exception-str-in-response | `raise HTTPException(500, str(e))` | logged internally, generic client message |
| Rate limiting on public endpoints | patterns.md, security | NO | T3 | py-fastapi-rate-limit (labeled: presence-of-limiter not reliably detectable) | — | — |
| radon maintainability index ≥ B; vulture dead-code; bandit SAST; pip-audit SCA wired into check/CI | tooling.md | NO | T2 | py-fastapi-quality-tooling-configured | pyproject lacks bandit/vulture/radon/pip-audit wiring | all four configured in pyproject/pre-commit |
| import-linter layer contracts must be configured (`[tool.importlinter]`) | tooling.md | NO | T2 | py-fastapi-importlinter-configured | no `[tool.importlinter]` contracts | contracts present |
| Coverage fail-floor configured (`fail_under`) | tooling.md, testing | PARTIAL (coverage referenced broadly; verify a py `fail_under`-required rule) | T2 | py-fastapi-coverage-fail-under | no `[tool.coverage.report] fail_under` | fail_under set |
| uv mandatory (no pip/poetry/pipenv) | tooling.md | PARTIAL (lockfile/pyproject required; no "uv-only" enforcement) | T2 | py-fastapi-uv-only | `requirements.txt`/`poetry.lock` present | `uv.lock` only |
| Test names must be behavioral `test_<action>_<scenario>_<outcome>` | testing.md | NO | T2 | py-fastapi-behavioral-test-names | `def test_order_1()` / `test_create()` | `test_cancel_order_already_shipped_raises_invalid_transition` |
| Every FSM transition tested (valid AND invalid) | testing.md | PARTIAL (validators/exception-path tests exist; not FSM-transition-coverage) | T2 | py-fastapi-fsm-transition-coverage | FSM defined, no test hitting invalid transition | test asserts `InvalidTransitionError` |
| Use factories for test data — no inline hardcoded dicts | testing.md | NO | T2 | py-fastapi-test-data-factories | inline `{"email": "...", ...}` dict in test | factory fixture used |
| Time/deadline tests must inject a clock — no wall-clock assertion | testing.md | PARTIAL (sleep-based-tests forbidden; no wall-clock-delta-assert rule) | T1 | py-fastapi-no-wallclock-assert | `assert time.monotonic() - start <= 0.6` | injected `FakeClock`, asserts on decision |
| Boolean names must use `is_/has_/can_/should_` prefixes | typing-style.md | NO | T2 | py-fastapi-boolean-naming | `active: bool` param/attr | `is_active: bool` |
| `is None`/`is not None`, not `== None` | typing-style.md | NO | T1 | py-fastapi-eq-none | `if x == None:` | `if x is None:` |
| `isinstance()` not `type(x) == T` | typing-style.md | NO | T1 | py-fastapi-type-equality | `if type(x) == int:` | `isinstance(x, int)` |
| f-strings for interpolation (not `%`/`.format()`); but NOT in log calls | typing-style.md | NO | T2 | py-fastapi-string-formatting | `"x %s" % v` / `"{}".format(v)` | f-string (or lazy `%s` in logger) |
| `zip(..., strict=True)` for parallel iteration | typing-style.md | NO | T2 | py-fastapi-zip-strict | `zip(a, b)` without `strict=` | `zip(a, b, strict=True)` |
| Class attributes / module vars must be annotated | typing-style.md | PARTIAL (functions-must-be-typed exists; not class/module attrs) | T2 | py-fastapi-attribute-annotations | unannotated class attribute | annotated |
| `raise ... from err` to preserve chains | typing-style.md | NO | T2 | py-fastapi-raise-from | re-raise in `except` without `from` | `raise NewError() from err` |
| Domain exception hierarchy (subclass a base `DomainError`) | typing-style.md | NO | T2 | py-fastapi-domain-exception-base | exception subclasses `Exception` directly in domain | subclasses `DomainError` |
| No `__` double-underscore name mangling unless necessary | typing-style.md | NO | T2 | py-fastapi-name-mangling | `self.__x` attribute | `self._x` |
| SOLID/SRP: class described with "and" / mixed responsibilities | solid-principles.md | NO | T3 | py-fastapi-srp (labeled: semantic, not mechanically checkable) | — | — |
| OCP/DIP: services depend on repo Protocol/ABC, not concretes | solid-principles.md, architecture | NO | T1 | py-fastapi-service-depends-on-protocol | `__init__(self, repo: UserRepository)` concrete class | typed against `Readable`/`Protocol` |
