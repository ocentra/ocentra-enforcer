# Parity gaps — linters-frontend-rust-cfml

Delta only: ADBP normative rules with NO or PARTIAL backing in `rules/rules.json`. Fully-backed rules omitted.
Registry has NO `cfml`/`coldfusion` language rules at all — every CFML row is NO.

| ADBP point | ADBP source | Backed? (family or NO/PARTIAL) | Tier | Proposed ruleId | Fail-fixture | Pass-fixture |
|---|---|---|---|---|---|---|
| `features/<A>` MUST NOT import `features/<B>` (cross-feature sideways import) | react-nextjs/check_feature_boundaries.ts | NO (grep cross-feature=0; SRC/DOMAIN don't model feature slices) | T1 | FE-FEAT-1.1 | `features/a/x.ts` importing `@/features/b/svc` | same import extracted to `@/shared/svc` |
| `components/` MUST NOT import `features/*` (layer inversion, shared UI depends on feature) | react-nextjs/check_feature_boundaries.ts | NO | T1 | FE-FEAT-1.2 | `components/Btn.tsx` importing `@/features/cart/x` | `components/Btn.tsx` importing only `@/lib/*` |
| TS/TSX file > 200 lines is blocking (frontend track) | react-nextjs/check_file_length.ts | PARTIAL (SRC-2.1 generic line budget; no 200 ts limit, no grandfather ratchet) | T1 | FE-LEN-1.1 | 201-line `.tsx` | 200-line `.tsx` |
| TS/TSX function/component > 30 lines is blocking | react-nextjs/check_file_length.ts | NO (no function-length rule for TS; SRC-2.1 is file-only) | T1 | FE-LEN-1.2 | 31-line arrow component | 30-line component |
| File-length grandfather ratchet: baselined file warns at/below baseline, fails if grown | react-nextjs/check_file_length.ts, file_length.rs | NO | T2 | FE-LEN-2.1 | baselined file grown +1 line | baselined file at baseline count |
| Only `src/lib/env.ts` may access `import.meta.env`; all others import validated `env` | vite-react/check_no_direct_env_access.ts | NO (import.meta.env grep=0) | T1 | FE-ENV-1.1 | `import.meta.env.VITE_X` in a component | `import { env } from "@/lib/env"` |
| No `fetch`/axios/api-client call inside a `useEffect` body (use TanStack Query) | vite-react/check_no_useeffect_for_fetching.ts | NO (useEffect=0, TanStack=0) | T1 | FE-EFFECT-1.1 | `useEffect(() => { fetch(...) })` | fetch via `useQuery` |
| Every `useEffect(...)` has a leading `// WHY:` justification comment | vite-react/check_useeffect_has_why.ts | NO | T1 | FE-EFFECT-1.2 | `useEffect` with no WHY comment | `// WHY: ...` above `useEffect` |
| Rust file > 400 lines is blocking (Rust-specific limit) | rust-cli-mcp/checks/file_length.rs | PARTIAL (SRC-2.1 applies to *.rs but at generic budget, not 400) | T1 | RUST-LEN-1.1 | 401-line `.rs` | 400-line `.rs` |
| Rust `domain/` MUST NOT import clap/rmcp/reqwest/hyper/axum/tokio::net/tokio::fs/std::process/std::io/crate::{cli,commands,tools,server} | rust-cli-mcp/checks/layer.rs | PARTIAL (DOMAIN-* rules are TS-language; no rust domain-import forbidden-crate rule) | T1 | RUST-LAYER-1.1 | `use reqwest;` in `src/domain/x.rs` | pure `src/domain/x.rs` |
| Rust `domain/` MUST NOT use `print!/println!/eprint!/eprintln!` (no I/O in domain) | rust-cli-mcp/checks/layer.rs | PARTIAL (RR-4.3 bans println/eprintln globally but not scoped/reported as domain-purity; covers macros, misses `print!`/`eprint!`) | T2 | RUST-LAYER-2.1 | `print!` in `src/domain/x.rs` | domain file with no print macros |
| MCP stdio server (rmcp dep): no stdout writes anywhere in src (`println!`,`print!`,`io::stdout`,`stdout()`) | rust-cli-mcp/checks/mcp.rs | PARTIAL (RR-4.3 catches println/eprintln; misses `io::stdout`/`stdout()`; not rmcp-gated/SKIP-aware) | T1 | RUST-MCP-1.1 | `io::stdout().write` in rmcp-dep crate src | writes routed to stderr/tracing |
| Module with public fn(s) MUST have tests (inline `#[cfg(test)]` or `tests/<mod>*.rs`); main/lib/build exempt | rust-cli-mcp/checks/companion.rs | PARTIAL (TEST-2.1 is crate-level scaffold; no per-module public-fn companion check) | T1 | RUST-TEST-1.1 | `pub fn` module, no inline/external test | same module with `#[cfg(test)]` |
| CFML unscoped function var / invalid var name (singleton race) | coldfusion/.cflintrc MISSING_VAR/VAR_INVALID_NAME | NO (no cfml lang rules) | T1 | CFML-VAR-1.1 | function local without `var`/`local.` | properly `var`-scoped local |
| CFML SQL without `cfqueryparam`/queryparam (SQLi) | coldfusion/.cflintrc QUERYPARAM_REQ/CFQUERYPARAM_REQ | NO | T1 | CFML-SQL-1.1 | `<cfquery>` interpolating a variable | same query using `<cfqueryparam>` |
| CFML function must not write to a shared/global scope | coldfusion/.cflintrc GLOBAL_VAR | NO | T1 | CFML-VAR-1.2 | fn writing `application.x` | fn writing only `local.` |
| CFML untyped public boundary (arg type/hint, component/function hint & return type) | coldfusion/.cflintrc ARG_TYPE_MISSING/ARG_HINT_MISSING/COMPONENT_HINT_MISSING/FUNCTION_HINT_MISSING/FUNCTION_TYPE_MISSING | NO | T1 | CFML-TYPE-1.1 | public fn missing arg type + hint | fully typed/hinted public fn |
| CFML dead code: unused local var / unused method argument | coldfusion/.cflintrc UNUSED_LOCAL_VARIABLE/UNUSED_METHOD_ARGUMENT | NO | T1 | CFML-DEAD-1.1 | fn with unused local | fn with no unused locals |
| CFML complexity: complex boolean (>3), nested cfoutput, nested complexity (>4), function too complex | coldfusion/.cflintrc COMPLEX_BOOLEAN_CHECK/NESTED_CFOUTPUT/FUNCTION_TOO_COMPLEX + params | NO | T2 | CFML-CPLX-2.1 | boolean expr with 4 operands | expr with ≤3 |
| CFML forbidden constructs: createObject, evaluate, iif, cfinclude tag | coldfusion/.cflintrc AVOID_USING_CREATEOBJECT/EVALUATE/IIF/CFINCLUDE_TAG | NO | T1 | CFML-BAN-1.1 | code using `evaluate(...)` | equivalent without evaluate |
| CFML size: method > 30 lines, component > 200 lines, > 5 args, excessive functions, var name too long | coldfusion/.cflintrc METHOD_TOO_LONG(30)/COMPONENT_LENGTH(200)/EXCESSIVE_ARGUMENTS(5)/EXCESSIVE_FUNCTIONS/VAR_TOO_LONG | NO | T1 | CFML-SIZE-1.1 | 31-line method / 6-arg fn | 30-line method / 5-arg fn |
| CFML `views/**/*.cfm` must not contain `<cfquery`/`queryExecute(` (view purity) | coldfusion/build/Linter.cfc view-purity | NO | T1 | CFML-LAYER-1.1 | view `.cfm` with `<cfquery>` | view calling a gateway |
| CFML `handlers/**/*.cfc` must not run queries (layer direction) | coldfusion/build/Linter.cfc layer-direction | NO | T1 | CFML-LAYER-1.2 | handler running `queryExecute` | handler delegating to service |
| CFML handlers/models must not `createObject("component",…Service/Gateway)` or `new …Service()/…Gateway()` (use WireBox DI) | coldfusion/build/Linter.cfc no-hardwired-deps | NO (wirebox grep=0) | T1 | CFML-DI-1.1 | handler `new FooService()` | collaborator injected via WireBox |
| CFML handler/model file > 200 lines | coldfusion/build/Linter.cfc file-length | NO | T1 | CFML-LEN-1.1 | 201-line handler `.cfc` | 200-line handler `.cfc` |
| Explicit hard stub with no `DEFERRED(#ref)` marker is a violation (raise NotImplemented, "...not implemented", UnimplementedError, unconditional/deferral skip); `@abstractmethod` exempt | deferred-tracker/deferred_scan.py --check | PARTIAL (SRC-1.2/RR-4.5 ban todo/unimplemented!/placeholder; no DEFERRED-marker grammar, no marked-stub exemption, no abstractmethod carve-out) | T1 | DEFER-1.1 | `raise NotImplementedError` with no marker | `DEFERRED(#123): reason [revisit: when]` above stub |
| Malformed `DEFERRED(#ref): reason [revisit: ...]` marker is a violation | deferred-tracker/deferred_scan.py (Marker.valid) | NO | T1 | DEFER-1.2 | `DEFERRED: no ref` | `DEFERRED(#12): why [revisit: v2]` |
| Soft deferral signals (TODO/FIXME, "deferred"/"for now"/"vN stub" prose) fail under `--strict` | deferred-tracker/deferred_scan.py --strict | PARTIAL (SRC-1.2 bans TODO always, not the scored/advisory --strict-gated soft-signal tier) | T2 | DEFER-2.1 | `# TODO: for now` prose (strict on) | no soft-signal prose |
