# Parity Gaps — Ergonomics (self-improving harness, orchestrator, telemetry, CI parity, loop-resilience)

Delta of ADBP normative invariants with NO / PARTIAL backing in `rules/rules.json`.
Existing `parity`/`budget`/`coverage` families cover the enforcer's OWN CI (Node suite, CLI/MCP parity, file/function line budgets) — none cover the orchestrator trust-but-verify gates, subagent dispatch blocks, loop telemetry/resilience, or the always-on context budget. Those are the gaps below.

| ADBP point | ADBP source | Backed? (family or NO/PARTIAL) | Tier | Proposed ruleId | Fail-fixture | Pass-fixture |
|---|---|---|---|---|---|---|
| Gate A: never ship a phase on a subagent's self-reported number — re-derive by re-running the full gate and reading the tooling's own summary line | orchestrator-verification.md §Gate A | NO | T1 | ORCH-1.1 | orchestrator/phase-report cites "789 passed" with no re-run/gate-output artifact | phase report includes captured tooling summary line (`N passed, M failed`) from an orchestrator re-run |
| Gate A: treat "already implemented / nothing to do" as UNVERIFIED — check against `git diff <base>...HEAD --stat` | orchestrator-verification.md §Gate A | NO | T1 | ORCH-1.2 | report says "already implemented before this session" with a non-empty diff stat present | "nothing to do" claim accompanied by empty `git diff --stat`, or claim removed |
| Gate A: a report-vs-re-run discrepancy is a finding → re-dispatch or HALT (never ship) | orchestrator-verification.md §Gate A | NO | T2 | ORCH-1.3 | reported count ≠ re-derived count, phase marked complete | reported count matches re-derived count |
| Gate B: stage from `git status --porcelain`, not `git diff` — new untracked source modules must not be silently dropped | orchestrator-verification.md §Gate B | NO | T1 | ORCH-1.4 | staging script/log uses `git add $(git diff <base> --name-only ...)`; untracked `*.py/.ts/...` source exists | staging derives file list from `git status --porcelain` (or `git add -A` after plan/lockfile committed) |
| Gate B: run a COMMITTED-TREE import/collection smoke before declaring phase green (stash -u → collect → pop) | orchestrator-verification.md §Gate B | NO | T1 | ORCH-1.5 | phase declared green with no committed-tree smoke (no `git stash -u`/`--collect-only`/`compileall` evidence) | committed-tree smoke run recorded before green |
| Gate B: state working-tree-vs-committed-tree divergence in the phase report | orchestrator-verification.md §Gate B | NO | T2 | ORCH-1.6 | phase report omits the local-vs-CI tree divergence note | report contains the divergence statement |
| Gate C: detect any commit on the working branch ahead of base — subagent must not have committed | orchestrator-verification.md §Gate C | NO | T1 | ORCH-1.7 | commits exist ahead of phase base at verification time with no reconciliation record | zero commits ahead of base, or a documented reconcile |
| Gate C: reconcile a subagent commit via `git reset --soft <base>` (dissolve, don't discard); never rewrite pushed history | orchestrator-verification.md §Gate C | NO | T1 | ORCH-1.8 | subagent commit hard-reset/discarded, or pushed history rewritten | unpushed subagent commit soft-reset and re-authored by orchestrator; pushed one accepted-and-verified |
| Gate C: state the reconciliation in the phase report | orchestrator-verification.md §Gate C | NO | T2 | ORCH-1.9 | subagent commit reconciled with no report line | report states "subagent created N commit(s)… reconciled via soft reset" |
| Orchestrator behavior file must not be pasted into subagent prompts (evaluator instructions ≠ generator instructions) | orchestrator-verification.md (intro) | NO | T2 | ORCH-1.10 | subagent prompt contains orchestrator-verification gate text | subagent prompt contains only quality-blocks content |
| Quality Block 1 (STOP-on-vulnerability) pasted verbatim into every implementation subagent prompt | quality-blocks.md §Block 1 | PARTIAL (secret-scan/injection rules exist for enforcer source, not for subagent-prompt assembly) | T1 | DISP-1.1 | dispatch prompt missing the SECURITY STOP block | dispatch prompt contains verbatim Block 1 |
| Quality Block 2 (iteration discipline: linter=oracle, stop after 2 non-converging attempts, transient-flake re-run) pasted into subagent prompt | quality-blocks.md §Block 2 | NO | T1 | DISP-1.2 | dispatch prompt missing iteration-discipline block | dispatch prompt contains verbatim Block 2 |
| Quality Block 3 (self-verification checklist) appended at END of subagent prompt | quality-blocks.md §Block 3 | PARTIAL (TODO/size families exist for enforcer source; not enforced as a required prompt block) | T1 | DISP-1.3 | dispatch prompt missing the stack self-verify checklist | checklist present at end of prompt |
| Quality Block 4 (git boundary) pasted DEAD LAST, after Block 3 and any addendum | quality-blocks.md §Block 4 | NO | T1 | DISP-1.4 | git-boundary block absent, or not the final block in the prompt | git-boundary block is the last block |
| Fix-track addendum: append "violation pattern returns ZERO matches" checklist line for fix phases | quality-blocks.md §Fix-track addendum | NO | T2 | DISP-1.5 | `/implement-fix-phase` dispatch lacks the zero-match line | fix dispatch includes the zero-match verification line |
| Loop-resilience: `statusline-meter.sh` writes `.harness/context-meter.json` (used/remaining %) and delegates to global statusline | loop-resilience/README.md | NO | T2 | LOOP-1.1 | resilience installed but statusline wrapper doesn't emit context-meter.json | context-meter.json produced with used/remaining fields |
| Loop-resilience: `loop-compaction-flag.sh` drops `.harness/compaction-pending` breadcrumb on PreCompact; never blocks compaction | loop-resilience/README.md | NO | T1 | LOOP-1.2 | PreCompact hook missing or blocks/prevents compaction | PreCompact hook writes compaction-pending and returns non-blocking |
| Loop-resilience: hooks act only when `.harness/` exists (inert otherwise) | loop-resilience/README.md | NO | T2 | LOOP-1.3 | hook runs unconditionally regardless of `.harness/` presence | hook guarded on `.harness/` existence |
| Loop-resilience: installed per-project by `/init-component` with explicit consent — never by `deploy.sh` | loop-resilience/README.md | NO | T2 | LOOP-1.4 | resilience hooks shipped/wired by deploy.sh, or installed without consent gate | agent-installed, consent-gated, deploy ships reference template only |
| Loop-resilience: every iteration re-hydrates from `.harness/loop/state.json` + git + STATUS (don't trust memory post-compaction) | loop-resilience/README.md | NO | T3 (behavioral; only labeled-presence checkable) | LOOP-1.5 | loop resumes from narrated memory with no state.json re-read | loop re-hydration reads state.json each iteration |
| Telemetry (P2a): one deterministic record per loop run appended to `.harness-archive/metrics.jsonl` | telemetry/README.md | NO | T1 | TEL-1.1 | completed loop run leaves no metrics.jsonl line | metrics.jsonl gains one record per run |
| Telemetry: record must carry required fields (command, mode, integration, phases_total/done, current_phase, degraded, complete, context_used_pct/remaining_pct, ts) | telemetry/README.md | NO | T1 | TEL-1.2 | metrics record missing any required field | record contains all required fields |
| Telemetry: aggregator is non-fatal / no-op with absent or corrupt state files (exit 0) | telemetry/README.md | NO | T1 | TEL-1.3 | aggregator raises on missing/malformed state.json | aggregator prints "no loop run" and exits 0 |
| Telemetry: numbers must be recorded deterministically, never LLM hand-counted (deterministic-script-not-LLM) | telemetry/README.md; SELF_IMPROVING_HARNESS P2b | NO | T2 | TEL-1.4 | friction counters populated from agent prose narration | counters sourced from persisted state events |
| Telemetry wired into `/handoff` Step 0d (every loop run leaves a line) | telemetry/README.md | NO | T2 | TEL-1.5 | `/handoff` has no telemetry-aggregate step | handoff Step 0d invokes aggregate_run |
| Always-on context METER (P7): tokenize always-on tier, print per-tier breakdown + total | context-budget/README.md | NO | T2 | CTX-1.1 | no meter script / no per-tier breakdown output | meter prints rules/shared + skill + agent description tiers and total |
| Always-on context BUDGET GATE (P8): fail CI when always-on tier exceeds committed ceiling in budget_baseline.json (ratchet on total_tokens + description_count) | context-budget/README.md | PARTIAL (file/function line budgets exist; no always-on context/description-count budget) | T1 | CTX-1.2 | `rules/shared/*` or description count grows past baseline without ceiling bump | tier within ceiling, or ceiling raised with logged reason |
| Budget ceiling only moves up via reviewed PR with written justification (loop must not raise its own ceiling) | SELF_IMPROVING_HARNESS §0; context-budget README | NO | T1 | CTX-1.3 | budget_baseline.json ceiling raised with empty/absent `reason` | ceiling change carries a justification string |
| Captured lessons route to on-demand tier (`rules/<stack>/` or agent do/don't) by default; `rules/shared/`+`CLAUDE.md` require an always-on justification (P9 routing) | SELF_IMPROVING_HARNESS P9; context-budget README | NO | T2 | CTX-1.4 | new `rules/shared/` entry with no always-on justification | lesson lands in stack tier, or shared entry carries justification |
| Safety invariant: loop proposes, human merges, model frozen — every change lands as a normal PR through CI + human review, never an auto-commit | SELF_IMPROVING_HARNESS §0 | NO | T3 (governance; labeled-presence only) | HARN-1.1 | harness self-change auto-committed without PR | change lands via reviewed PR |
| CI parity: CI runs the SAME checks at the SAME pinned tool versions as the local hook runner | ci/README.md | PARTIAL (enforcer's own Node/CLI-MCP parity backed; generic local-hook-vs-CI check-set + pinned-version parity not enforced for target projects) | T1 | CIP-1.1 | local hook runs checks/versions absent from the CI workflow (or vice-versa) | local runner and CI invoke identical check set at identical pinned versions |
| CI parity: use the ecosystem-idiomatic hook runner (pre-commit / lefthook / githooks+xtask) — don't force one onto the wrong stack | ci/README.md | NO | T2 | CIP-1.2 | Rust/CFML project uses `.pre-commit-config.yaml` for its gate | stack uses its idiomatic runner |
| CI recipe surface must be complete: format, lint, test, coverage floor, dep-audit, secret scan, local hook runner, CI parity | ci/README.md | PARTIAL (secret-scan + coverage families exist for enforcer; not a completeness gate over a target project's gate surface) | T2 | CIP-1.3 | project gate missing one of the required surfaces (e.g. no dep-audit) | all required gate surfaces present |
