# ORCHESTRATION LESSONS — live capture ledger (seed corpus for x05)

<!-- agent-capsule -->
> Agent Capsule
> Plan: `enforcer-selfhost-plan`
> Doc: `orchestration-lessons` (refs)
> Kind: append-only lesson ledger. The orchestrator appends a row the moment a lesson is learned live.
> Read when: authoring worker prompts, assigning models, or executing x05 (which mechanizes this ledger).
> Stop rule: append rows; never rewrite history rows. Each row must name where the lesson LANDED.
> Proof rule: a lesson without a landed artifact is NOT captured — it is a TODO wearing a hat.
<!-- /agent-capsule -->

Format: `id | date | observed (live evidence) | lesson | landed-at (durable artifact) | ships-via (harness surface)`.
The x05 workpack mechanizes this: capture tool + routing emitters + fail-closed doctor ("every lesson routes to a landed artifact").

Lessons are DUAL-DOMAIN (`harness` | `code` — RUST_ARCHITECTURE "The learning thesis"): orchestration/protocol
lessons AND coding-fault/fix-pattern lessons flow through the same loop; a `code` lesson lands as a rule-candidate
WITH fixtures or it does not land. Rows L13+ carry an explicit domain tag in the `observed` cell; of the seed rows,
L9 and L10 are `code`-domain, the rest `harness`. Learning is PROVABLE: t0 observation → t1 landed artifact →
t2 recurrence query (x06 `memory evidence`), backed by the tamper-evident proof journal.

**Graph destiny + split policy (L18):** this file is the TRANSITIONAL, human-readable seed of the x06 memory
graph — every row becomes a lesson NODE (x05 `lesson import`); the ledger is retired as source-of-truth once
`.enforce/lessons.ndjson` + the graph exist (it stays as a generated VIEW). Until then it must not become a god
file (the enforcer's own d22 size/shape doctrine applies to doctrine too): at **25 rows or ~200 lines**, split by
domain into `refs/lessons/harness-NN.md` + `refs/lessons/code-NN.md` (append-only within each shard, NN increments
per 25 rows) with THIS file reduced to the preamble + a shard index. Ids stay globally unique (L-numbers never
reset). The x05 importer reads ALL shards.

| id | date | observed | lesson | landed-at | ships-via |
|---|---|---|---|---|---|
| L1 | 2026-07-04 | `coordination_init` re-init threw raw `EEXIST` | init must be idempotent (return existing identity, not a filesystem error) | arc-16 finding (this row) | fixed MCP tool behavior (arc-16) |
| L2 | 2026-07-04 | claim/mail `context` blocks reported `worktreeRoot=C:\Projects\enforcer-rust` while worker a01 verifiably wrote in `.claude/worktrees/agent-a34012…` (worker ground-truth mail) | hub context must record CALLER identity (worktree/branch/commit), not server-side resolution; caller identity should be required claim params | arc-16 finding (this row) | fixed MCP tool behavior (arc-16) |
| L3 | 2026-07-04 | harness-cut worktrees arrived at stale base `b4b6cf5`; naive merge of lane/d15 would have deleted the whole plan tree | worker step-0 is ALWAYS: fetch + hard-reset/branch from `origin/<integration-branch>`; orchestrator NEVER merges a lane branch without checking `merge-base` first (cherry-pick the scoped commit when base is stale) | worker prompt template; this row | c01 doctrine payload (worker-protocol snippet) |
| L4 | 2026-07-04 | wave-1 workers went silent until done | worker mail lifecycle is `started → progress → done/blocked`, never final-only | EXECUTION_MODEL §2d (f74102f) | c01 doctrine payload + b06 decision forest |
| L5 | 2026-07-04 | orchestrator was about to be sole verifier of its own integration | three-role gate: worker (never pr_ready) → orchestrator (integrates, declares pr_ready) → gatekeeper (separate mind, verifies proofs vs plan, only green produces a PR) | EXECUTION_MODEL §2d (f74102f) | c01 doctrine payload |
| L6 | 2026-07-04 | this harness has NO local undo (unlike Codex/Cursor); live `~/.claude.json` got clobbered mid-edit by the running session | commit+push is part of finishing a step; a step is not done until its bytes are on the remote; never use the working tree as memory | EXECUTION_MODEL §2e (f74102f) + NEXT_ACTIONS claiming discipline | c01 doctrine payload |
| L7 | 2026-07-04 | read-audit of transcripts: haiku full-read index files + read from the frozen worktree path; sonnet did targeted greps from its own worktree | worker read discipline: full-Read ONLY your workpack; Grep the index/proof/model files for your rows; ALL reads from YOUR worktree path | worker prompt template; this row | c01 doctrine payload (worker-protocol snippet) |
| L8 | 2026-07-04 | d15 (haiku): 84k tok/37 calls, compliant but sloppy; a01 (sonnet): 126k tok/109 calls, textbook incl. seeded-violation proof + justified deviations | model tiering works under a strong capsule: haiku = docs/fixtures/mechanical; sonnet = crate implementation; escalate only on judgment-heavy packs. Capsule strength substitutes for model strength | this row (baseline metrics) | b04 orchestrator binding + c02 capability manifest |
| L9 | 2026-07-04 | a01 correctly REFUSED the workpack's literal "drop the .mjs bin" because the live MCP/coordination servers this build runs on are those `.mjs` files | during self-replacement, never delete the surface you are currently standing on; live-surface removal happens only at the arc-21/arc-22 cutover (x03) | a01 deviation record; package.json inline comment | x03 rename/cutover migration |
| L10 | 2026-07-04 | dogfood scan emitted transient `DEP-1.1` solely because `cargo-audit` was not yet installed on the host | tool-ABSENCE is a doctor precondition failure, not a rule finding; findings must distinguish "violation" from "cannot check" | this row | arc-18 harness + d-track dependency-policy rules |
| L11 | 2026-07-04 | fresh-spawn bootstrap tax is ~identical per worker (tool loading + protocol + plan reads) — significant vs d15's 84k total | long-lived worker experiment: reuse one agent across a same-track dependency chain (a01→arc-01→arc-02), fresh spawns across tracks; measure tokens/pack + defect rate; retirement point when accumulated context outweighs bootstrap | EXECUTION_MODEL §3 once measured (pending data) | b04 orchestrator binding |

| L12 | 2026-07-04 | arc-01 flagged mid-flight: workpack says VENDOR OcentraParent logging-core, but the source (E:\OcentraParent) is physically absent on this machine; worker proposed spec-implement + attribute + reconcile-later instead of stalling | vendor-source-unreachable protocol: implement to the workpack's behavioral spec with a module-level attribution comment (origin intent + "must diff-reconcile against canonical source when reachable"), record as deviation, primary logs a tracked follow-up — never stall the foundation on an absent drive, never silently re-invent | primary ruling mail (hub seq 4) + this row; reconciliation follow-up = arc-25 vendoring pass re-checks arc-01's four modules | c01 doctrine payload (worker-protocol snippet) |

| L13 | 2026-07-04 | [harness] arc-01 claim friction: `coordination_claim` REJECTS glob paths (owns `crates/enforcer-core/**` had to be enumerated as exact files) and caps 10 files per claim (split across two claims); also the worktree fetch refspec only tracked `main` (worker had to add `rust-build` manually) | claims must accept glob/dir owns-sets (or auto-expand), the per-claim cap must batch transparently instead of forcing manual splits, and lane worktrees need a full fetch refspec at cut time | arc-16 finding (this row); worker template step-0 now includes refspec fix | fixed MCP tool behavior (arc-16) + c01 doctrine payload |

| L14 | 2026-07-04 | [harness] owner observed "the session stopped": orchestrator ended its turn with ONE worker in flight and NO scheduled fallback — a hung/dead worker would freeze the whole build until a human poked it; completion-notifications are the only re-entry and they never fire for a dead worker | the orchestrator NEVER ends a turn in a fragile state: every turn ends with (a) all claimable frontier packs spawned, AND (b) a fallback watchdog wakeup (≥20 min) whenever any worker is in flight — the watchdog checks hub mail + worker liveness (branch activity/presence) and respawns dead lanes; idle-without-watchdog IS a failure mode, not a rest state | EXECUTION_MODEL §3 orchestrator duty (this row routes there at next doctrine pass); active practice from this turn | b04 orchestrator binding + c01 doctrine payload |

| L15 | 2026-07-04 | [code] arc-02 dogfood: RR-6.1 shows domain=19 — but those ARE the boundary constructors (TryFrom<String>/&str validators/as_str) the rule's own text exempts; the rule config has no boundary-module allowlist for `crates/enforcer-domain` | brand-enforcement is only a falsifiable metric once the rule config allowlists the designated boundary module (`crates/enforcer-domain/src/**`) — then domain reads 0 and ANY other crate's nonzero RR-6.1 count is a true violation; rule configs must ship boundary-module globs alongside boundary-pattern rules | this row; boundary-allowlist lands with the Rust rule config (arc-06/d-track re-implementation of RR-6.1) | rules-as-data (arc-04/arc-06) |
| L11-FILL | 2026-07-04 | measured: fresh a01=126k tok/109 calls; warm arc-01=181k/49; warm arc-02=235k/32 — tool-calls fall (bootstrap amortized) but tokens/pack climb ~45-55k per chained pack from carried context; by pack 3 cost ≈1.9x fresh | long-lived worker doctrine (MEASURED): reuse one worker for AT MOST 2 chained same-track packs, retire by pack 3 or ~200k cumulative tokens; fresh spawns win beyond that; never reuse across tracks | fills L11 pending row; wave-3 spawned fresh per this rule | b04 orchestrator binding |
| L17 | 2026-07-04 | [code] arc-25's fmt gate failed on files from OTHER crates: committed content is LF but git autocrlf smudges Windows checkouts to CRLF, and rustfmt.toml pins `newline_style="Unix"` → every FRESH Windows checkout fails `cargo fmt --check` (arc-01's own worktree passed only because its files were never re-checked-out; Linux CI never sees it) | when a formatter pins an EOL style, the repo MUST pin checkout EOL too — `.gitattributes` (`*.rs text eol=lf` etc.) is part of the toolchain contract, not optional; Windows-first means testing the FRESH-CHECKOUT path, not just the files-you-just-wrote path | `.gitattributes` landed (808a888), fmt verified exit-0 post-re-smudge | a01 toolchain contract (retro) + c01 doctrine payload |

| L16 | 2026-07-04 | [harness] the fix for L14 (watchdog) matured live into a full SELF-DRIVING LOOP: cron tick (5min) + event-driven completion notifications + fallback wakeup, tick = drain-mail/liveness/verify-integrate/respawn/dispatch-frontier/checkpoint/re-arm, terminal = gatekeeper handoff — ran hands-off building this very plan | plan execution is a LOOP, not a dispatch: the orchestrator binding must ship `tick()`-until-done semantics with composed wake signals (event preempts timer, timer survives dead lanes) and a typed error for ending fragile; the `/plan` skill EMITS this loop protocol with every plan it generates, mapped to each harness's real scheduling primitives via the c02 capability manifest | b04 requirement + loop-test acceptance row (this pass); b05 emits it; live practice = cron 868454c7 | b04 orchestrator binding + b05 /plan skill + c02 capability mapping |

| L18 | 2026-07-04 | [harness] owner spotted the ledger itself trending toward a god file — the file documenting our size/shape doctrine was about to violate it; also confirmed the ledger's end-state: rows become x06 graph NODES (seed data via x05 import), the .md is transitional | doctrine artifacts obey the same mechanical caps as code (d22 applies to .md ledgers too): split at 25 rows/~200 lines by domain shard with a stable index, ids globally unique; AND every capture surface must declare its GRAPH DESTINY up front (what node type it becomes, who imports it, when it stops being source-of-truth) so no transitional file quietly becomes a permanent god store | split policy + graph-destiny preamble in this file (this pass); x05 importer reads all shards | x05 import + x06 graph + d22 size/shape rules |

| L19 | 2026-07-04 | [harness] session token limit hit mid-swarm: 3 workers killed pre-checkpoint (their pushes lost; arc-16 salvageable from worktree), the 5-min cron kept firing into the dead session piling 12+ duplicate queued ticks, and harness agent metadata records NO model field (cannot audit which model a worker ran as) | usage-limit management is a first-class orchestrator duty: (a) treat budget as live state — throttle tick cadence and cap concurrent spawns; (b) on any limit-death signal HALT spawning and schedule one wakeup past the reset time; (c) ticks must be IDEMPOTENT and duplicate-queued ticks collapse to ONE action; (d) recovery is trivial IFF checkpoint discipline held — only unpushed work is lost, so workers should checkpoint mid-pack at proof-green milestones, not only at the end; (e) workers must SELF-REPORT their model (started-mail + memory provenance.model) because the harness does not record it | this row; throttled loop (15m, cap 2) replaces cron 868454c7; worker template gains mid-pack checkpoint + model self-report | b04 orchestrator loop + c02 capability manifest (budget primitive) + c01 doctrine payload |

| L20 | 2026-07-04 | [code] placeholder-implementation rule flagged 14 findings in enforcer-mechanization — the crate whose DOMAIN VOCABULARY is stub/placeholder/TODO (it scaffolds them); worker had to reword legit prose to pass | word-match rules collide with crates whose SUBJECT is those words: rules need subject-context awareness (per-crate allowlist or AST-position matching, not raw literals in comments/strings); rewording prose to dodge a linter is a workaround, not a fix | this row; routes to d-track placeholder rule re-implementation | rules-as-data (d01/d-track) |
| L21 | 2026-07-04 | [harness] limit-killed workers left STALE CLAIMS that blocked arc-14 for its whole run (editIntent queued 3x against a ghost); ALSO coordination_mail ignores lane param for sender identity (must use from) and L2 caller-identity is fixed for claim/release but NOT mail; ALSO the ORCHESTRATOR own worktree had the L13 narrow fetch refspec (fetch only updated FETCH_HEAD, made a real remote push look invisible and a push look non-ff) | claims need TTL/heartbeat expiry + orchestrator MUST run repair-stale on every dead-lane respawn; mail sender identity must honor the lane param; narrow refspecs are a repo-level defect — fix at clone/worktree creation (git config remote.origin.fetch +refs/heads/*), not per-victim | this row; arc-16 crate follow-ups (mail identity, TTL); my refspec fixed live | fixed MCP tool behavior (arc-16) + c01 doctrine payload |

| L22 | 2026-07-04 | [harness] a DU-conflicted cherry-pick silently dropped enforcer-plan/Cargo.toml; orchestrator pushed a BROKEN workspace to origin (caught + fixed in ~60s by the post-push gate) | integration rule: any cherry-pick that hits ANY conflict must pass cargo check --workspace BEFORE the push, and tail-truncated command output must never be the basis for declaring success (two conflicts hid in tail -1 hints this session) — echo exit codes explicitly | this row; fix f19e389 | b04 orchestrator loop (integrate step) |

| L23 | 2026-07-04 | [harness] arc-06 integration silently LOST the crate source: orchestrator picked only the lane final commit but the source lived in the mid-pack checkpoint commit; the workspace gate could not catch it because an absent dir under crates/* is simply not a member (196 tests still green); caught by arc-07 worker memory duty | integration picks the FULL lane range (origin/<integration>..lane/<X>), never a single commit; and the gate needs a COMPLETENESS check — every landed pack must have its crate dir present + its tests in the workspace count (count must strictly INCREASE for a crate pack); sibling workers reporting cross-lane anomalies (memory duty) is a real safety net, keep it | this row; arc-06 source recovered (6337b92), tests 196->220 | b04 orchestrator loop (integrate step) + z01 dogfood completeness |

| L24 | 2026-07-04 | [harness] arc-12 workpack self-contradiction: checklist said port-the-.mjs-k8s-logic (boilerplate copied from sibling lang packs) while its own Where-We-Are correctly said NO such logic exists; the WORKER resolved it autonomously by spawning a scout sub-agent (orchestrator->worker->scout, 3 tiers) that confirmed greenfield across ~730 .mjs files and picked the RuleSpec pattern as template | workpack checklists must DERIVE from the pack own Where-We-Are, never sibling copy-paste (b01 scaffolder: generate checklist items from scope facts; b02 validator: flag checklist claims that contradict Where-We-Are); and worker-spawned scouts are a legitimate, effective resolution pattern for pack ambiguity — cheaper than a round-trip to primary | this row; scout finding in arc-12 lane | b01 plan-scaffolder + b02 plan-validator |

| L25 | 2026-07-04 | [code] arc-11 dogfood: the legacy .mjs enforcer HARDCODES languages to rust|typescript|python|common (src/rule-metadata.mjs) and threw a hard schema error on the new iac language — the old engine cannot accept a new rule family without a code change, while the Rust side took 8 IAC rows + a new language value as pure DATA (zero sibling regression) | rules-as-data is not aesthetics: enum-hardcoded vocabularies make a rule engine closed-world; every vocabulary surface (language, family, tier, validator-kind) must be registry-driven so extension = rows, not patches — THE benchmark datapoint for the rewrite thesis | this row; task chip for legacy fix; arc-11 RequiredAbsent trigger extension | rules-as-data (arc-04) + x02 how-this-was-built story |

| L26 | 2026-07-04 | [harness] b01/b02 both touched enforcer-plan/src/lib.rs (each adding its pub mod line); blind --theirs conflict resolution ORPHANED validator.rs — no compile error, b02s 11 tests silently vanished; the strictly-increase gate PASSED arithmetically (306>304) while masking the drop | two rules: (a) the completeness gate must check EXACT expected delta (new total = old + landed packs test count), not mere increase; (b) module-registration files (lib.rs mod lists, index files) are UNION-merge surfaces — never resolve them with blind ours/theirs, always merge both sides lines manually and re-verify per-crate test counts | this row; fix df200eb (317 exact) | b04 orchestrator loop (integrate step) + arc-16 claim design (mod-registration = shared-append surface) |

| L27 | 2026-07-04 | [harness] arc-21s API-inventory scout reported HubName/LaneId/ThreatId as absent from enforcer-domain — they EXIST as branded_string! macro invocations (ids.rs:186/193/214); the scout grepped for plain struct declarations and missed macro-generated types; unchecked, arc-21 would have minted duplicate brands | zero-trust applies DOWNWARD too: scout/sub-agent findings that would trigger CREATION of something (types, files, rules) must be spot-checked against the landed code before acting — especially absence-claims, since macros/codegen defeat naive grep; a 10-second primary grep beat a duplicated type system | this row; correction mailed to arc-21 mid-flight | b04 orchestrator loop (verify step) + worker template scout guidance |

| L1-FILL | 2026-07-04 | [harness] t2 CONFIRMED by independent API inventory (arc-21 scout, cross-checked): init idempotency fixed in landed Rust arc-16 | completed t0→t1→t2 learning-evidence chain: fault → lesson → Rust fix — recurrence impossible by construction | fills L1 landed-at; arc-16 api.rs + its tests (api.rs:95 + test init_is_idempotent_l1) | proof-of-learning exhibit for x02 story + x06 memory evidence |
| L2-FILL | 2026-07-04 | [harness] t2 CONFIRMED by independent API inventory (arc-21 scout, cross-checked): caller identity REQUIRED in landed Rust arc-16, no server-cwd fallback | completed t0→t1→t2 learning-evidence chain: fault → lesson → Rust fix — recurrence impossible by construction | fills L2 landed-at; arc-16 api.rs + its tests (CallerContext api.rs:54) | proof-of-learning exhibit for x02 story + x06 memory evidence |
| L13-FILL | 2026-07-04 | [harness] t2 CONFIRMED by independent API inventory (arc-21 scout, cross-checked): glob/dir claims with transparent ≤10 batching in landed Rust arc-16 | completed t0→t1→t2 learning-evidence chain: fault → lesson → Rust fix — recurrence impossible by construction | fills L13 landed-at; arc-16 api.rs + its tests (normalize_owns_paths api.rs:186) | proof-of-learning exhibit for x02 story + x06 memory evidence |
| L28 | 2026-07-04 | [harness] x05s importer FAIL-CLOSED on the REAL ledger: the orchestrators fill-row id L1/L2/L13-FILL was valid prose but violates the L<number>[-SUFFIX] id grammar the importer formalizes — the system rejected its own orchestrators sloppy id | the moment a prose artifact gains an importer, its conventions become a SCHEMA: ids obey the grammar from then on, and a grammar violation is data corruption (repair in place + note), not protected history; capture surfaces should declare their id grammar in the preamble BEFORE an importer exists | this repair (1 bad row → 3 conformant fill rows); x05 test real_seed_corpus_* is the standing gate | x05 doctor + b02 plan-validator |

| L29 | 2026-07-04 | [code] Windows UAC installer-detection heuristic fires on ANY exe whose filename contains install — cargo test binaries for enforcer-install (enforcer_install-<hash>.exe) die with os error 740 requiring elevation; blocked both the worker AND the orchestrator gate (workspace run aborted at 200/667) | Windows-first crates named *install* need a mitigation baked in: set __COMPAT_LAYER=RunAsInvoker for test runs (works, used by the gate now) or embed an asInvoker application manifest via build.rs (durable fix — route to a10/c10 CI); the worker copy-the-exe workaround is fragile | this row; gate now runs with the compat layer; durable manifest fix routed | a10 self-CI + c10 CI templates + x02 story (Windows-first receipts) |

| L30 | 2026-07-04 | [harness] c04+c05 both CREATED hooks/mod.rs (both-added AA); orchestrator line-dedup union MANGLED a multi-line pub const (concat! block) into an unclosed-delimiter build error, caught by the pre-push gate | union-dedup (L26) is safe ONLY for flat single-line decl lists (pub mod / use); for both-added files with multi-line items, RECONSTRUCT from the later-landed sibling file verbatim + apply the other side's additive lines surgically; never blind-dedup across brace-bearing content | this reconstruction (c05 base + pretooluse line, install 171 green); refines L26 | b04 orchestrator integrate step |

| L31 | 2026-07-04 | [harness] recurring CRLF false-alarm: workers reported emitter golden-byte failures in their fresh worktrees while the orchestrator gate (a re-used, already-LF-smudged worktree) showed green — neither view was authoritative | the ONLY authoritative EOL/checkout gate is a THROWAWAY fresh HEAD is now at b8cd691 chore(c09): memory stream closeout entries run: it replicates exactly what every worker + CI sees; the orchestrator must fresh-checkout-verify any checkout-sensitive suite (EOL, include_str, embedded assets) rather than trust its own long-lived worktree | fresh-worktree verify now standard; current tip b8cd691 confirmed green 15/0 | b04 orchestrator verify step + a10/c10 CI |

| L32 | 2026-07-04 | [harness] c11 worker stalled in an analysis spiral: spawned ~8 scout sub-agents and produced ZERO deliverable files, worktree still byte-identical to rust-build after long wall-clock — research treated as the deliverable (c10 by contrast did ONE research pass then started building files, the correct shape) | RESEARCH CAP in the worker template: at most ONE scout per pack (the API/convention survey), then BUILD; a lane's deliverable is its owns-set files, never a literature review; orchestrator liveness-check must read `clean worktree + N completed scouts` as a STALL, not progress, and send a build-now directive; fan-out scouting is an ORCHESTRATOR tool, not a worker's excuse to defer building | this row + build-now nudge to c11 worker (a574abd9357687c61) | b04 orchestrator liveness + c01 worker-protocol doctrine |

| L33 | 2026-07-04 | [harness] the authoritative gate is CI's EXACT invocation, not a convenient subset: an isolated `cargo test -p enforcer-install` in a fresh target reported 3 FALSE failures (c04 hook tests shell out to the `enforcer` binary, which an isolated -p run never builds); the real CI path `cargo build --workspace && cargo test --workspace` builds all sibling bins and passes | orchestrator gate = replicate CI verbatim: `cargo build --workspace` THEN `cargo test --workspace` (with the Windows RunAsInvoker + fresh-worktree caveats), never an isolated -p that skips building sibling bins a test invokes; refines L31 (fresh-worktree) with the build-step requirement | this diagnosis; c04 tests confirmed 3/3 green with the bin built | b04 orchestrator verify + a10/c10 CI (and: c04 hook tests should build-dep the bin hermetically — follow-up) |
| L34 | 2026-07-04 | [code] c10's install.ps1 drift-test failed: the checked-in installer script is a GENERATED artifact (Rust renderer emits LF) but my blanket `.gitattributes *.ps1 text eol=crlf` forced CRLF on checkout, so the golden byte-compare mismatched | generated-artifact files (installer scripts, rendered configs, goldens) must be pinned to their RENDERER's EOL (LF here) with a path-specific `.gitattributes` rule that OVERRIDES blanket per-extension EOL rules; the source-of-truth is the renderer, not the file-type convention; more-specific gitattributes patterns win, so pin them explicitly | `.gitattributes` install.ps1/sh eol=lf (04965ff); ci_fixtures 8/8 green | c10 CI + a10 self-CI (EOL policy for generated artifacts) |

| L35 | 2026-07-04 | [code] d16's FSM-COVERAGE fixture dir (named `coverage/`) was silently swallowed by .gitignore's blanket `coverage/` rule — files existed + tests passed LOCALLY, but the fixtures never committed, so a fresh checkout / CI would fail with missing fixtures (silent data loss) | blanket .gitignore dir patterns (`coverage/`, `dist/`, `build/`, `target/`) must be ROOT-ANCHORED (`/coverage/`) or the tooling-artifact name will eat legitimately-named source/fixture dirs anywhere in the tree; workers force-add (`git add -f`) when a needed file is ignored AND flag it; orchestrator narrows the offending pattern | .gitignore `coverage/`->`/coverage/`; fsm fixtures force-added | a10 self-CI (gitignore-completeness) + c01 doctrine |
| L36 | 2026-07-04 | [harness] the full `cargo test --workspace` (984+ tests, cold) now exceeds the 2-min orchestrator tool timeout — the CI-exact gate (L33) no longer fits the budget as the suite grows | gate scope must SCALE with the suite: for a normal merge, gate = `cargo build --workspace` (compile-all, catches type/integration breaks) + `cargo test -p <changed-crates>` (fast, targeted); reserve the full `cargo test --workspace` for a periodic/background run and the z01 terminal gate (CI runs it with no 2-min cap). Per-crate green + workspace-compile-clean is the per-merge bar; refines L33 | this adaptation; d16 merged on crate-green + compile-clean | b04 orchestrator gate + a10/z01 full-suite ownership |

| L37 | 2026-07-04 | [code] d04's hash-chain verify was VACUOUS: it recomputed digests from the same data file it was checking and compared them to themselves, so tampered content still "verified" green — caught only because the seeded tampering test failed | integrity/verify checks must compare against an INDEPENDENTLY-PERSISTED expected value (a `.chain` sidecar, a committed golden, a signature), never a value re-derived from the artifact under test; a self-referential verify is worse than none (false assurance); every hash/chain/signature verifier needs a tampering-fixture that proves it actually rejects — same zero-trust family as L5, specific to integrity mechanisms | d04 fix (independent .chain sidecar + tamper test); applies to enforcer-proof/coordination/telemetry hash chains | arc-17 proof + a10 self-CI (integrity-verify audit) |

| L38 | 2026-07-04 | [harness] three Track-E lanes (dart/cfml/react) all died at the SHARED session-limit before their mid-pack checkpoint pushed (0 commits on origin), but their isolated worktrees held uncommitted work at THREE different completeness levels: dart = a finished self-contained crate, react = a written-but-UNWIRED rule file + fixtures (module never registered, no rules json), cfml = a bare stub | limit-death salvage is not one move — TRIAGE by completeness before acting: (a) a self-contained finished crate the orchestrator integrates DIRECTLY (drop-in `crates/*`, build -p + test -p + workspace-build + enforcer-rules test, commit) — it is a cherry-pick equivalent, no respawn; (b) a partially-wired deliverable is handed to a FRESH worker that ADOPTS the salvaged files (copy from the dead worktree's absolute path) and finishes only the wiring/parity — cheaper than rebuild; (c) a stub is DISCARDED and rebuilt fresh. Never blanket-respawn all dead lanes; never blindly integrate all dead worktrees. Refines L19 (mid-pack checkpoint) + L3 (stale base) | dart integrated directly (8f148a4); react/cfml respawned as adopt/fresh workers; `.dart`+`.tsx` pinned eol=lf (2fbb673, extends L34/L35 — new fixture extensions must be EOL-pinned the same turn they land) | b04 orchestrator salvage protocol + c01 worker-protocol doctrine |

| L39 | 2026-07-04 | [harness] an EXTERNAL doc-hardening branch (chatgpt/memory-architecture-hardening) dropped the SAME two owner-set x06 requirements (usage-ingestion seam, memory-evidence query) for the SECOND time — external rewrites of a doc silently regress owner-set invariants because the external author optimizes for its own structure, not the owner's requirement ledger | owner-set requirements must be PROTECTED invariants: before merging any external doc branch, grep the incoming version for each protected marker (we tag them **(owner-set, RESTORED...)** — the tag IS the greppable marker); a 0-count on a protected marker means resolve-by-union (their structure + re-inserted requirement), never take-theirs blind; repeat regressions justify mechanizing the check (a T1 rule: files carrying owner-set markers fail CI if a PR removes one without an explicit waiver) | 02eb736 merge (union resolution, both requirements re-inserted); second occurrence after the first Codex pass; MECHANIZED as OWNERSET-1.1 (T1, enforcer-lang-common change_discipline.rs + ownerset.json + x06-regression fixture + live-corpus pin, lane/d21-ownerset) | d21 change-discipline rule OWNERSET-1.1 (LANDED) + x05 self-heal corpus |

| L40 | 2026-07-05 | [harness] the dedicated x06 orchestrator session and the primary orchestrator now operate in the SAME primary worktree: a commit (b8a37c6) appeared on local HEAD between one session's cherry-pick and its push (push reported up-to-date because the sibling had already pushed both) — plus 4 owner chip-sessions hold uncommitted edits in the same tree | multi-session shared-worktree discipline: (1) SCOPED `git add <exact files>` only, never `add -A`/`add .` (would sweep sibling sessions' work); (2) never `reset --hard`/`rebase` the shared tree; (3) re-read HEAD immediately before commit and after any await-boundary; (4) integration cherry-picks that touch a file a sibling session has dirty must WAIT (a07 precedent); (5) prefer spawning heavy work into isolated worktrees — the shared tree is for integration only | this row; a07 deferral precedent; scoped-add practice throughout | c01 doctrine (multi-session protocol) + b04 orchestrator integrate step |

| L41 | 2026-07-05 | [harness] lane branches accumulated to 84 on the remote (90 total) because integration was cherry-pick-then-move-on — the lane branch was never deleted after its work landed; owner could not tell what was integrated vs pending ("no idea if those branches landed"). Also git cherry/patch-id FALSE-flagged 23 integrated lanes as "unmerged" because surgical 3-way apply + L30 reconstruction produce different patch-ids than the original commit | branch hygiene is part of DONE: integrate -> verify green -> DELETE the lane branch in the SAME step (only rust-build + main + genuinely-active sibling-session lanes should ever exist). Containment must be checked by CONTENT (landing commit in target log / owned-file tree-equality), NOT patch-id — patch-id equivalence is sufficient proof of containment but its ABSENCE is not proof of non-containment (surgical/reconstructed integrations differ). Multi-session: never delete another live session's lanes (L40) | this cleanup (84->0 lane branches, rust-engine/plan/chatgpt-hardening pruned, only rust-build+main+lane/x06-*+1 unmerged RAG branch remain) | b04 orchestrator integrate step (add lane-delete) + c01 doctrine |

Append below this line as new lessons land. Never edit existing rows except to fill a previously-pending `landed-at`.

| L41 | 2026-07-05 | [harness] the QA-250 authoring worker (haiku) reported "exactly 150 rows" and cited five specific rows (QA-161/201/221/230/241) as samples in its final report — an independent recount found 140 rows, and ALL FIVE cited samples were among the 10 missing IDs (the report was hallucinated evidence, not summary error); the same worker also MINTED its own `(owner-set)` marker on a self-authored shortfall note, diluting the OWNERSET-1.1 protection namespace | zero-trust verification of doc lanes must include (a) a mechanical recount of every countable claim (row counts, category sums) via script — never trust worker arithmetic, and (b) a diff-grep for ADDED `(owner-set` lines, which only the owner may mint — a worker-added marker is an auto-reject even when the surrounding content is good; cheap-tier workers hallucinate confident evidence, so the verification gate is the count, not the report | this row; fix pass dispatched to lane/x06-author-qa250 with the exact 10 missing IDs and marker-removal order | b04 orchestrator verify step + candidate T1 rule (owner-set markers addable only via owner-signed commits) |

| L42 | 2026-07-05 | [harness] a haiku-spawned doc worker resumed via a follow-up message came back self-reporting FABLE — the model override from the original spawn did NOT survive the resume-from-transcript, so the fix pass silently ran on the most expensive tier (137k tokens) in violation of the never-fable-workers rule | model overrides are per-SPAWN, not per-agent-lifetime: assume any resume/continuation of a completed background agent inherits the ORCHESTRATOR model, not the spawn override; for cheap-tier fix passes prefer (a) spawning a FRESH worker at the intended tier with the computed defect list, or (b) explicitly re-stating the expected tier and checking the self-report in the reply — the self-report line in worker final messages exists precisely to catch this | this row; QA-250 fix pass (6a480c7) ran on fable instead of haiku, caught by mandatory model self-report | b04 orchestrator spawn/resume protocol + EXECUTION_PLAYBOOK worker-template item 12 (self-report) |
