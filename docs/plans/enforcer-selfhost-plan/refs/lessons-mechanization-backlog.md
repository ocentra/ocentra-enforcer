# Lessons Mechanization Backlog

<!-- agent-capsule -->
> Agent Capsule
> Plan: `enforcer-selfhost-plan`
> Doc: `lessons-mechanization-backlog` (refs)
> Kind: generated sweep artifact; read only when executing x05 (lesson-mechanization) or auditing ledger-to-code coverage.
> Read when: prioritizing x05 self-heal work, or checking whether a lesson's `ships-via` mechanism has actually landed.
> Stop rule: this is a SNAPSHOT from the d21-ownerset sweep (2026-07-04). Re-derive rather than hand-edit stale rows; update the `landed?` column in place as mechanisms ship.
> Proof rule: a row here with `landed? = NO` is not itself a violation — it is the input list x05 works from.
<!-- /agent-capsule -->

Generated by the d21-ownerset lane's sweep duty: grep every row of
[`orchestration-lessons.md`](./orchestration-lessons.md) whose `ships-via` cell names a
mechanism (crate, module, rule, CI wiring — as opposed to a pure prose/doctrine reference),
and check whether that mechanism has a landed code artifact in the repo today. This is a
LIST, not a build — closing NO rows is x05's job, not this lane's.

A row is marked `landed? = YES` only when a concrete file/module implementing the named
mechanism was found. `PARTIAL` means the `ships-via` cell names multiple mechanisms and at
least one component is real code while another (most often a bare "c01 doctrine payload" /
narrative-doc reference) has no code or CI artifact. `NO` means nothing beyond the ledger
row's own prose exists — the mechanism is a TODO wearing a hat (per this ledger's own proof
rule).

| lesson id | ships-via | landed? | owning pack (existing code / follow-up) |
|---|---|---|---|
| L1 | arc-16 finding (this row) | PARTIAL (self-referential row; real fix lands at L1-FILL) | arc-16 (`crates/enforcer-coordination/src/api.rs`) |
| L1-FILL | arc-16 `api.rs` + test `init_is_idempotent_l1` | YES | arc-16 (`crates/enforcer-coordination/src/api.rs`) |
| L2 | arc-16 finding (this row) | PARTIAL (self-referential row; real fix lands at L2-FILL) | arc-16 (`crates/enforcer-coordination/src/api.rs`) |
| L2-FILL | arc-16 `api.rs` + `CallerContext` | YES | arc-16 (`crates/enforcer-coordination/src/{api,lib}.rs`) |
| L3 | worker prompt template; c01 doctrine payload | NO | c01 (doctrine text only lives in `EXECUTION_MODEL.md`/workpack docs; no code/CI enforces the fetch/hard-reset protocol) |
| L4 | EXECUTION_MODEL §2d + c01 doctrine payload + b06 decision forest | PARTIAL (b06 real; doctrine text prose) | b06 (`crates/enforcer-plan/src/agents_forest.rs`) landed; c01 doctrine follow-up |
| L5 | EXECUTION_MODEL §2d + c01 doctrine payload | NO | c01 (no gatekeeper-role code found) |
| L6 | c01 doctrine payload | NO | c01 (doc-only) |
| L7 | c01 doctrine payload (worker-protocol snippet) | NO | c01 (doc-only) |
| L8 | b04 orchestrator binding + c02 capability manifest | PARTIAL (b04 real; c02 missing) | b04 (`crates/enforcer-plan/src/orchestrator.rs::tick()`) landed; c02 capability/budget manifest module not found anywhere |
| L9 | x03 rename/cutover migration | YES | x03 (`crates/enforcer-install/src/migrate_legacy_name.rs`, `adapters/codex.rs`) |
| L10 | arc-18 harness + d-track dependency-policy rules | YES | arc-18 (`crates/enforcer-harness`) + dependency-policy rows in `crates/enforcer-rules/rules/*.json` |
| L11 | b04 orchestrator binding (pending data) | YES (mechanism landed; row's own measurement caveat is separate) | b04 (`orchestrator.rs` tick/frontier) |
| L11-FILL | b04 orchestrator binding | YES | b04 (`orchestrator.rs`) |
| L12 | c01 doctrine payload | NO | c01 (doc-only) |
| L13 | arc-16 finding (this row) + c01 doctrine payload | PARTIAL (self-ref + doc; real fix lands at L13-FILL) | arc-16 |
| L13-FILL | arc-16 `api.rs` + `normalize_owns_paths` | YES | arc-16 (`crates/enforcer-coordination/src/{api,lib}.rs`) |
| L14 | b04 orchestrator binding + c01 doctrine payload | PARTIAL (b04 real; c01 doc-only) | b04 (`orchestrator.rs` watchdog/tick) |
| L15 | rules-as-data (arc-04/arc-06) | YES | arc-04/arc-06 (`crates/enforcer-rules/rules/*.json`, 29+ catalogs) |
| L16 | b04 + b05 /plan skill + c02 capability mapping | PARTIAL (b04 real; b05, c02 missing) | b04 (`orchestrator.rs`) landed; no `/plan` skill dispatch code, no capability-mapping module |
| L17 | a01 toolchain contract + c01 doctrine payload | YES | a01/c01 (`.gitattributes` — `*.rs text eol=lf` etc.) |
| L18 | x05 import + x06 graph + d22 size/shape rules | YES | x05 (`crates/enforcer-plan/src/lessons.rs`), x06 (`crates/enforcer-memory/src/graph.rs`), d22 (`size-shape.json`) |
| L19 | b04 orchestrator loop + c02 capability manifest (budget) + c01 doctrine | PARTIAL (b04 real; c02, c01 missing) | b04 (`orchestrator.rs`) landed; budget/capability-manifest primitive not found |
| L20 | rules-as-data (d01/d-track) | YES | d01 (`crates/enforcer-mechanization/src/{scaffold,oracle}.rs`) |
| L21 | fixed MCP tool behavior (arc-16) + c01 doctrine payload | PARTIAL (arc-16 crate substantial; mail/TTL specifics + c01 doc not individually confirmed) | arc-16 (`crates/enforcer-coordination`) |
| L22 | b04 orchestrator loop (integrate step) | YES | b04 (`orchestrator.rs`) |
| L23 | b04 orchestrator loop + z01 dogfood completeness | PARTIAL (b04 real; z01 doc-only) | b04 landed; z01 (`workpacks/z01-dogfood-proof-gate.md`) not wired into `.github/workflows/*.yml` |
| L24 | b01 plan-scaffolder + b02 plan-validator | YES | b01/b02 (`crates/enforcer-plan/src/{scaffolder,validator}.rs`, both cite L24 by name) |
| L25 | rules-as-data (arc-04) + x02 how-this-was-built story | PARTIAL (arc-04 real; x02 story missing) | arc-04 landed; x02 narrative doc does not exist yet (only its spec `workpacks/x02-docs-refresh.md`) |
| L26 | b04 orchestrator loop + arc-16 claim design | PARTIAL (b04 real; arc-16 claim-design specifics not individually confirmed) | b04 (`orchestrator.rs` merge/integrate) |
| L27 | b04 orchestrator loop (verify step) + worker template scout guidance | PARTIAL (b04 real; scout guidance doc-only) | b04 landed; c01 doctrine follow-up |
| L28 | x05 doctor + b02 plan-validator | YES | x05/b02 (`lessons.rs::run_doctor` + `validator.rs`) |
| L29 | a10 self-CI + c10 CI templates + x02 story | PARTIAL (CI real; x02 story missing) | a10/c10 (`.github/workflows/ci.yml`) landed; x02 narrative doc missing |
| L30 | b04 orchestrator integrate step | YES | b04 (`orchestrator.rs`) |
| L31 | b04 orchestrator verify step + a10/c10 CI | YES | b04 + a10/c10 (`orchestrator.rs`, `.github/workflows/ci.yml`) |
| L32 | b04 orchestrator liveness + c01 worker-protocol doctrine | PARTIAL (b04 real; c01 doc-only) | b04 landed |
| L33 | b04 orchestrator verify + a10/c10 CI | YES | same as L31 |
| L34 | c10 CI + a10 self-CI (EOL policy) | YES | a10/c10 (`.gitattributes` — `install.ps1`/`install.sh` `eol=lf` override) |
| L35 | a10 self-CI (gitignore-completeness) | YES | a10 (`.gitignore` — `/coverage/` root-anchored) |
| L36 | b04 orchestrator gate + a10/z01 full-suite ownership | PARTIAL (b04 real; z01 doc-only) | b04 landed; z01 full-suite CI ownership still a spec |
| L37 | arc-17 proof + a10 self-CI (integrity-verify audit) | YES | arc-17 (`crates/enforcer-proof/src/journal.rs` — real hash-chain + tamper-fixture tests) |
| L38 | b04 orchestrator salvage protocol + c01 worker-protocol doctrine | PARTIAL (b04 plausible; c01 doc-only) | b04 (`orchestrator.rs`) |
| L39 | d21 change-discipline rule (owner-set-marker preservation) + x05 self-heal corpus | YES | d21 (`crates/enforcer-rules/rules/ownerset.json`, `crates/enforcer-lang-common/src/rules/change_discipline.rs`, `crates/enforcer-rules/tests/ownerset_registry.rs` — landed THIS pack) |

## Summary

- **Fully landed (`YES`): 20 rows.**
- **Partial (`PARTIAL`, at least one named mechanism still missing/prose-only): 15 rows.**
- **Pure prose (`NO`, nothing beyond the ledger row itself): 4 rows** (L3, L5, L6, L7, L12 — all bare `c01 doctrine payload` references with no code/CI artifact).

## Recurring gaps (close these once, not per-row)

- **`c02 capability manifest` / budget primitive** — named in L8, L16, L19; no module found anywhere in the workspace. One landed module would close three rows at once.
- **`b05 /plan skill` dispatch code** — named in L16; no skill-dispatch implementation found (only doc references to a `/plan` skill).
- **`z01 dogfood-completeness` CI wiring** — named in L23, L36; exists only as `workpacks/z01-dogfood-proof-gate.md` (a spec), not wired into `.github/workflows/*.yml`.
- **`x02` "how this was built" narrative doc** — named in L25, L29; only its authoring spec (`workpacks/x02-docs-refresh.md`) exists, not the story doc itself.
- **Bare `c01 doctrine payload` references** — the single largest recurring gap (L3, L5, L6, L7, L12, and a component of nine further PARTIAL rows). These are orchestration/worker-protocol lessons that live only as prose in `EXECUTION_MODEL.md` / workpack text, never mechanized as a rule, test, or CI check. Most are judgment-heavy protocol lessons (three-role gate, read discipline, commit-before-done) that may legitimately stay T3-advisory rather than becoming T1 rules — x05 should triage each for "mechanizable check" vs. "durable doctrine label," not assume all of them need code.

x05 should use this table as its intake list: close the recurring-gaps modules first (each closes multiple rows), then triage the remaining bare-doctrine rows individually.
