# TEST_PROOF_EXPECTATIONS

<!-- agent-capsule -->
> Agent Capsule
> Plan: `enforcer-selfhost-plan`
> Doc: `Test Proof Expectations`
> Kind: index / contract doc. This is the proof-tier authority every workpack routes into before DONE.
> Read when: You are about to close a workpack (select its proof tier + name its proof rows here), OR you are authoring/reviewing a workpack's "Acceptance And Proof" section, OR you need to know what P0-P5 mean.
> Stop rule: This doc defines proof *tiers and obligations*. It does not itself prove any workpack. A workpack is only DONE when its own proof rows here are green — not when this doc is read.
> Proves: the mapping workpack-type -> required proof tier -> concrete test/artifact. Nothing else.
> Does not prove: that any test currently passes; the runtime status lives in WORKPACK_INDEX.md and the proof rows below.
> Proof rule: Every workpack MUST select a proof tier from P0-P5 via the decision tree, and MUST name its concrete test file(s) / artifact path(s) in a proof row here before it moves to DONE.
<!-- /agent-capsule -->

Sources: [WORKPACK_INDEX](./WORKPACK_INDEX.md), [PROOF_INDEX](./PROOF_INDEX.md), [PLAN_EXECUTION_BLUEPRINT](./PLAN_EXECUTION_BLUEPRINT.md), [PLAN_STATE](./PLAN_STATE.md).

---

## 1. Doctrine this doc enforces

Rules are conditions. **Enforcement MUST be mechanical.** Prose without a backing check is hope, not proof. This doc is where every workpack is forced to name the *mechanical* check that backs its claim of DONE.

The plan's mechanization ladder (the tier a workpack's *rule* sits at) is distinct from the proof tier (the *test shape* that proves it):

- **T1** — hard/deterministic validator. Fail-closed. ruleId<->validator<->doc<->fixtures parity. A T1 claim MUST be proven by a test that trips on a seeded violation and passes on a conforming input.
- **T2** — scored/advisory but still mechanical (regex/AST/heuristic emitting `score`+`confidence`, non-blocking; the Rust literal-scan model). A T2 claim MUST be proven by a test asserting the score is in `[0,1]`, carries a confidence, and never changes exit code.
- **T3** — justified prose, only when mechanization is impossible; MUST be labeled `advisory, no mechanization possible + <reason>`. A T3 claim is proven only by a **labeling gate** (a T1 check that the label is present) — never by trusting the prose.

Every ADBP borrow is dragged UP this ladder, never copied as prose. If a borrow lands as T3, the T3 label itself is mechanically enforced.

### 5-way tested-enforcement parity (the load-bearing invariant)

A rule is only "enforced" when it is **tested**. The doctrine, stated plainly: **an untested validator does not count as enforcement.** A validator with no fail-fixture that trips it and no pass-fixture that stays clean is unproven code, not a gate. Every rule the plan ships (and every rule the d01 mechanization engine scaffolds) MUST satisfy the parity for its T-tier before its proof row can go GREEN:

- **Every T1 rule** requires the full **5-way chain**: `ruleId <-> doc <-> validator <-> {fail-fixture + pass-fixture} <-> detection-test`. All five artifacts must exist and cross-resolve; the detection test must assert the fail-fixture is flagged AND the pass-fixture stays clean. A missing link at any of the five points fails the d01 `rule-scaffold-parity` oracle closed.
- **Every T2 rule** requires fixtures that assert the **score behavior against a threshold**: a fail-fixture whose `score` **crosses** the fail threshold and a pass-fixture whose `score` **stays under** it (the Rust literal-scan scored model), plus a confidence value and zero effect on exit code. The chain is the same 5-way shape; the detection test asserts the threshold crossing rather than a hard block.
- **Every T3 rule** requires only the enforced **LABEL**: the rule doc carries the verbatim `advisory, no mechanization possible + <reason>` label, and the *presence of that label* is itself T1-enforced by the d01 parity oracle. No behavioral fixture — but the label gate is mandatory and mechanical.

This 5-way parity is exactly what the new Track D families (d16-d18, d21-d23, d25-d28), the Track E language/universal packs (including the OPTIONAL opt-in `e-pack-crypto-blockchain`), the Track H money-critical/security-testing mandate (h01-h08), and the terminal z01 dogfood gate are built to satisfy: every new ruleId is scaffolded through d01 so its ruleId, doc anchor, validator, fail+pass fixtures, and detection test all exist and cross-resolve, or it does not count.

---

## 2. Proof tiers P0-P5

The proof tier answers: **what shape of evidence closes this workpack?** Higher number = heavier, more integrated proof. A workpack picks exactly one *primary* tier (it may carry secondary rows, e.g. a T1+T2 workpack lists both a hard-gate row and a score row).

| Tier | Name | What it proves | Required evidence | Fail-closed? |
|------|------|----------------|-------------------|--------------|
| **P0** | Contract / schema | A type, schema, brand, template, or interface has the exact declared shape; illegal shapes are rejected at compile/decode time. | A `cargo test -p <crate>` compile-reject fixture (a bare type where a branded newtype is required must not type-check) OR a serde decode test (valid mints, invalid rejects) OR a frozen-snapshot template test. No runtime execution required. | Yes — an illegal shape MUST fail to compile/decode. |
| **P1** | Unit | A single module/function does what it says over inputs incl. edge/adversarial cases. | A `cargo test -p <crate>` unit/integration test with pass + fail + edge cases; the crate additionally passes `cargo clippy` and `cargo fmt --check`. | Yes for T1 rules; observers may be non-gating. |
| **P2** | CI / cross-platform | The check runs identically as a CI job and locally, across the supported toolchain and OS matrix (Win/macOS/Linux, incl. musl + apple-silicon). | A CI job invokes `cargo test`/the gate; a test asserts determinism and platform-independence (path normalization, seeded skew fails). | Yes — CI job exits non-zero on violation. |
| **P3** | Live MCP-tool | A live MCP tool / server path behaves correctly against a running (or faithfully stubbed) transport. | A test invokes the MCP tool path (e.g. `mcp_status`/freshness) and asserts observable behavior; mutate an input, re-observe the change. | Yes for T1-scoped tool behavior. |
| **P4** | Self-enforce green | The enforcer enforces *itself*: run the real gate against this repo/plan and it must be honestly green (nonzero ran-count, no hollow pass), and a seeded self-violation must fail it. | Run the built `enforcer` binary (`xtask dogfood`) / the PLAN-* validator / the parity oracle against the live tree; assert green with visible ran-counts AND a seeded violation fixture makes it exit non-zero. | Yes — a hollow (zero-ran) or bypassed run MUST fail. |
| **P5** | Install / integration proof | An install/adapter/hook produces the correct on-disk or runtime result end-to-end against a real (temp) fixture home, round-trips, and fails closed on corruption. | Against a temp fixture: `install`->`verify` all-green; corrupt input -> `verify` fails; `install`->`uninstall` restores pre-state; for hooks, a seeded payload yields the exact deny/allow + ruleId+fix. | Yes — corruption or a seeded violation MUST be caught. |

Notes:
- Workpacks in the tree label their tier with the *proof* tier vocabulary (e.g. `P1 unit`, `P0 contract/schema`, `P5 install-proof`) and, where the rule is scored/advisory, also name the T-tier of the rule. The two vocabularies co-exist by design: P-tier = evidence shape, T-tier = mechanization ladder.
- A doc-only workpack (d15) has **no runtime tier**: its proof is artifact existence + cross-link integrity, and the doctrine honesty guardrail is that it is *explicitly labeled doc-only, gating nothing* (no prose masquerading as a check).

---

## 3. Decision tree: workpack type -> required proof

Walk top to bottom; take the first branch that matches.

```
START
 |
 |-- Is the deliverable ONLY documentation/prose (no validator, gates nothing)?
 |      YES -> DOC-ONLY: proof = artifact exists + every claim cross-links a source.
 |             MUST be labeled T3 "advisory, no mechanization possible: <reason>"
 |             or "doc-only, gates nothing". (d15, d14 content layer, d09 persona layer)
 |             The LABEL is enforced at P0/T1 even when the content is T3.
 |      NO  -> continue
 |
 |-- Does it define a type / brand / schema / template / adapter interface (no runtime behavior)?
 |      YES -> P0 contract/schema: cargo compile-reject fixture OR serde decode test OR frozen snapshot.
 |             (a03-a06 brands, c08 stubs, b03 templates, d12 AST-rule contracts, d14 labeling)
 |      NO  -> continue
 |
 |-- Is it a Cargo-workspace crate build (dependency-ordered, behavior in the crate)?
 |      YES -> P1 unit: `cargo test -p enforcer-<crate>` green over the crate,
 |             clippy/fmt clean, an injected clippy/type error -> non-zero.
 |             (all arc-01 .. arc-25, plus the 3 E-built lang crates dart/cfml/crypto)
 |      NO  -> continue
 |
 |-- Is it a single validator / oracle / engine with deterministic pass/fail?
 |      YES -> P1 unit (T1): pass + fail + edge fixtures; seeded violation trips it;
 |             ruleId<->validator<->doc<->fixture parity holds; fails closed on missing input.
 |             (d01-d04, d06-d10, d13, a08)
 |      NO  -> continue
 |
 |-- Does it emit a score/confidence (heuristic, non-blocking)?
 |      YES -> P1 unit (T2): score in [0,1], confidence present, exit code unchanged,
 |             asserted against fixtures. Pair with a T1 row if it also hard-ratchets.
 |             (d05 score, d10 smells)
 |      NO  -> continue
 |
 |-- Must it match between local and CI, or run across the OS/toolchain matrix?
 |      YES -> P2 CI cross-platform: CI job runs `cargo test`/the gate; determinism + seeded-skew-fails test.
 |             (d05 ratchet, d11, d28)
 |      NO  -> continue
 |
 |-- Does it exercise a live MCP tool / server / transport path?
 |      YES -> P3 live MCP-tool: invoke the tool path, mutate input, re-observe.
 |             (a02 fingerprint-over-dist)
 |      NO  -> continue
 |
 |-- Does it install / write config / register a hook into a harness home?
 |      YES -> P5 install-proof: temp-home install->verify green; corrupt->fail;
 |             install->uninstall restores; hooks: seeded payload -> exact deny/allow+ruleId+fix.
 |             (c03-c06 adapters+hooks)
 |      NO  -> continue
 |
 |-- Does it turn the enforcer/validator on ITSELF (this repo or this plan dir)?
 |      YES -> P4 self-enforce green: real gate vs live tree, honestly green with ran-counts,
 |             seeded self-violation fails. No --no-verify bypass; waivers are the only exception.
 |             (a09, a10, b02 [validates THIS plan dir], b05, z01 terminal gate)
 |      NO  -> re-read this tree; every workpack MUST land in exactly one primary tier.
END
```

Ambiguity rule: if two branches both match, pick the **higher-numbered** tier as primary (heavier proof) and list the lower as a secondary row. Example: c07 defines an interface (P0) but also writes a config file and re-reads disk (would be P5-ish) — it is scoped P1 unit because it is pure over an injected filesystem abstraction; its adapter siblings that touch a real home are P5.

---

## 4. Proof rows

Every workpack registers its concrete proof here before DONE. Columns:
`Workpack | Proof tier(s) | Named test / oracle | Artifact path | Seeded-violation (fail) case | Status`.
Status starts `PENDING`; the closing agent flips it to `GREEN` only when the named test passes AND (for T1/P4/P5) the seeded-violation case is shown to fail. `GREEN` here is the *only* thing that authorizes a WORKPACK_INDEX status move to DONE.

### Track A - crate-build swarm (arc-01 .. arc-25)

All P1 unit, T-tier N/A (mechanical Cargo-workspace crate build). Uniform proof: `cargo test -p enforcer-<crate>` green over the crate; `cargo clippy` and `cargo fmt --check` clean; the crate compiles and its detection/parity tests pass before the row goes GREEN. The 25 arc packs stand up 25 of the 28 workspace crates; the remaining 3 lang crates (`enforcer-lang-dart`/`-cfml`/`-crypto`) are BUILT by the Track E `e-pack-*` packs and prove `cargo test -p` in their own Track E rows.

| Workpack | Proof tier | Named test / oracle | Artifact path | Seeded-violation case | Status |
|----------|-----------|---------------------|---------------|-----------------------|--------|
| arc-01..24 (each) | P1 unit | `cargo test -p enforcer-<crate>` + `cargo clippy`/`cargo fmt --check` clean | `proof/cargo/arc-<NN>.txt` | inject a clippy/type error in an owned crate -> `cargo test -p`/clippy non-zero | PENDING |
| arc-25 (`enforcer-events`) | P1 unit | `cargo test -p enforcer-events` (VENDORED `ocentra-eventing`, renamed) + envelope stored-decode/contract boundary tests + `cargo clippy`/`cargo fmt --check` clean | `proof/cargo/arc-25.txt` | a stored `EventEnvelope<E>` whose contract hash != the decoded event's contract -> boundary re-verify decode rejects; a panicking handler NOT isolated by Sequential/Concurrent dispatch -> test fails; inject a clippy/type error -> `cargo test -p`/clippy non-zero | PENDING |
| WAVE-2/3 decomposed rows (note) | P1 unit (per owning crate) | Each `arc-16/17/18/03` sub-row and each `arc-06/07/08/09/10` per-prefix rule inventory is proven by its crate's `cargo test -p <crate>` as already stated in those packs — NO separate proof row per sub-item | (the owning crate's `proof/cargo/arc-<NN>.txt`) | the **count-parity assertion** is the mechanical gate: the crate's declared per-prefix rule inventory count must equal the registered `ruleId`/validator/fixture set; a mismatch fails `cargo test -p <crate>` closed | PENDING |

`arc-02` (`enforcer-domain`, the keystone schema) additionally proves each branded newtype with a fail-fixture + pass-fixture and a serde round-trip test (invalid value -> decode error; valid value -> round-trips), re-checked by every downstream crate that types against it. `arc-25` (`enforcer-events`) is VENDORED from OcentraParent's `ocentra-eventing` as-is (renamed, ocentra-specific deps swapped for `enforcer-domain`/`enforcer-core`); its boundary/contract tests carry over and its consumers are arc-15/arc-16/arc-17. Each crate's `cargo test -p` must be green (and clippy/fmt clean) before any crate that depends on it is accepted.

### Track A - domain packs (a01 .. a10)

| Workpack | Proof tier(s) | Named test / oracle | Artifact path | Seeded-violation case | Status |
|----------|--------------|---------------------|---------------|-----------------------|--------|
| a01 | P1 unit | `cargo build` + `cargo test`/`cargo clippy`/`cargo fmt --check`/`cargo deny check`/`cargo audit` contract test + `[workspace.lints]` deny-wall schema test (`rust-toolchain.toml`/`deny.toml` present) | `proof/cargo/a01-toolchain.txt` | seed a `todo!()`/`unwrap()` in a lint-opted crate -> `cargo clippy` non-zero; drop a `[lints] workspace=true` -> deny-wall test fails | PENDING |
| a02 | P3 live MCP-tool | `mcp-fingerprint-over-artifacts` (live `mcp_status`/freshness over the built binary/crate artifacts) | `proof/mcp/a02-fingerprint.json` | mutate a built artifact -> digest changes; unbuilt -> `exists:false` | PENDING |
| a03 | P0 contract/schema (T1) | `ruleid-brand-decode` serde round-trip + ruleId<->registry parity + `cargo test -p enforcer-domain` compile-reject (bare `String` in a `RuleId` field) | `proof/schema/a03-ruleid.txt` | bare `String` where `RuleId` required -> does not type-check; unknown id -> decode `Err` | PENDING |
| a04 | P0 contract/schema (T1) | `path-brand-decode` serde round-trip + compile-reject fixture (Win+POSIX) | `proof/schema/a04-paths.txt` | absolute-as-relative / `..` escape -> decode fails; `RelPath` for `RepoRoot` -> does not type-check | PENDING |
| a05 | P0 contract/schema (T1) | `sha256-brand-decode` round-trip + compile-reject fixture | `proof/schema/a05-sha256.txt` | wrong length/case/charset -> decode fails; bare `String` in a `Sha256` field -> does not type-check | PENDING |
| a06 | P0 contract/schema (T1) | `coordination-id-brand-decode` + compile-reject fixture | `proof/schema/a06-coordids.txt` | unsafe charset/empty/oversize -> fails; `HubName` vs `LaneId` swap -> does not type-check | PENDING |
| a07 | P0 contract/schema (T1) | `parse-at-boundary` serde decode tests + `no std::env outside env-boundary` grep/AST + `no untyped serde_json::Value in routing` | `proof/schema/a07-boundary.txt` | malformed/schema-invalid JSON -> decode error naming file; env read outside boundary -> gate trips | PENDING |
| a08 | P1 unit (T1) | `waiver-honesty` (`waivers.ron`) config test + count parity | `proof/config/a08-waivers.json` | a bare limit-bump or empty-reason waiver -> fails; waived+fixed count parity mismatch -> fails | PENDING |
| a09 | P4 self-enforce green (T1) | `anti-silent-skip` scanner test + `enforcer scan --self` ran-count | `proof/self/a09-scan.txt` | unmatched-ext / missing-tool / empty-selection -> explicit `skipped:<reason>` not silent pass | PENDING |
| a10 | P4 self-enforce green (T1) | `real-self-enforcement` (`xtask dogfood` + `enforcer`'s Rust rules on its own crates) + `dogfood.yml` workflow gate | `proof/self/a10-ci.txt` | seed a self-violating fixture -> `enforcer` self-run + CI non-zero; hollow zero-ran -> CI fails | PENDING |

### Track C - install / enforce (c01 .. c09)

| Workpack | Proof tier(s) | Named test / oracle | Artifact path | Seeded-violation case | Status |
|----------|--------------|---------------------|---------------|-----------------------|--------|
| c01 | P1 unit (T1) | `install-core-contract` | `proof/install/c01-contract.json` | `--dry-run` writes any file -> fails; unknown adapter id -> error not silent skip | PENDING |
| c02 | P1 unit (T1) | `harness-autodetect` (temp-home fixtures) | `proof/install/c02-detect.json` | empty home -> no false positive; env-override precedence wrong -> fails | PENDING |
| c02 (capability manifest) | P1 unit | `cargo test -p enforcer-install` (`harness-autodetect` capability-manifest rows over `tests/fixtures/detect/caps-codex/**`, `caps-claude-bare/**`) | `proof/install/c02-detect.json` | Codex-marker fixture (`agents/openai.yaml` with `allow_implicit_invocation`) must emit the six primitives with `implicitInvocation: Yes` + strong `crossSessionMessaging`; a bare-`.claude` fixture must declare unprovable fields as `Unknown`/`Support::Unknown` (fail-closed, never guessed `Yes`); an empty-home fixture emits no manifests; any field guessed `Yes` without a marker -> fail. | PENDING |
| c03 | P5 install-proof (T1) | `claude-adapter-install` (temp `~/.claude.json`) | `proof/install/c03-claude.json` | corrupt `~/.claude.json` -> `verify` fails; install->uninstall doesn't restore -> fails | PENDING |
| c03 (agent descriptor) | P5 install-proof | `cargo test -p enforcer-install` (`claude-adapter-install` descriptor rows over `tests/fixtures/claude_adapter/**`) | `proof/install/c03-claude.json` | pass: `install` writes `.claude/agents/<server-name>.md` with valid frontmatter and `verify` confirms the descriptor; fail: descriptor absent or hand-corrupted -> `verify` returns a typed error (not skip); round-trip `install`->`uninstall` restores pre-state byte-for-byte (descriptor removed). | PENDING |
| c04 | P5 install-proof (T1 bridge) | `claude-deny-hook-blocks` | `proof/install/c04-denyhook.json` | seeded violating edit -> exit=deny with exact `ruleId`+`fix`; conforming -> allow; T2-only -> allow-with-warning | PENDING |
| c05 | P5 install-proof (T1) | `claude-sessionstart-injects` (snapshot) | `proof/install/c05-sessionstart.txt` | reminder body drift vs snapshot -> fails; missing T1/T2/T3 tokens -> fails | PENDING |
| c06 | P5 install-proof (T1) | `codex-adapter-parity` (golden TOML + `AGENTS.md`) | `proof/install/c06-codex.txt` | generated block != current `codexMcpTomlBlock`/`globalAgentsInstructionBlock` -> fails | PENDING |
| c06 (agent descriptor) | P5 install-proof | `cargo test -p enforcer-install` (`codex-adapter-parity` `agents/openai.yaml` golden row over `tests/fixtures/codex/**`) | `proof/install/c06-codex.txt` | pass: install writes the native `agents/openai.yaml` descriptor equal to the pinned snapshot, sourced from the native emitter (no reliance on the bulk `.codex-plugin` publish); fail: generated `openai.yaml` diverges from the pinned snapshot / is missing / comes from the bulk publish path -> build fails. | PENDING |
| c07 | P1 unit (T1) | `generic-writer` + `install-doctor` (golden + fixtures) | `proof/install/c07-generic.json` | missing/renamed server file -> doctor red naming the check | PENDING |
| c08 | P0 contract/schema (T3-labeled) | `adapter-stub-contract` | `proof/install/c08-stubs.json` | stub applies writes a file / doesn't return `status:"deferred"` / registry miss -> fails | PENDING |
| c09 | P5 install-proof (T1) | `remaining-adapters-detect` (autodetect enumerates all six ids; doctor aggregates per-adapter checks over temp-home fixtures) | `proof/install/c09-remaining-adapters.json` | JSON-config harness (antigravity/windsurf/kilocode/kiro) with server entry missing/renamed on disk -> `verify` reports the named failing check; second apply not byte-identical -> non-idempotent fail; CLI-only harness (opencode/aider) missing the T3 `deferred: no mcp surface` label or writing any file -> fails; absent harness not returning `skipped:not-detected` -> silent-skip fail | PENDING |

### Track D - ADBP borrows rebuilt mechanically (d01 .. d15, then d16-d18, d21-d23, d25-d28)

| Workpack | Proof tier(s) | Named test / oracle | Artifact path | Seeded-violation case | Status |
|----------|--------------|---------------------|---------------|-----------------------|--------|
| d01 | P1 unit (T1) | `cargo test -p enforcer-mechanization` (`rule-scaffold-parity` + `rule-new`) | `proof/rules/d01-parity.txt` | unknown validator / dangling doc anchor / missing fixture / orphan -> parity fails closed | PENDING |
| d02 | P1 unit (T1) | `cargo test -p enforcer-scan` (`baseline-ratchet`) | `proof/rules/d02-baseline.json` | one added finding fails; grown count fails; ratchet can't silently expand | PENDING |
| d03 | P1 unit (T1) | `cargo test -p enforcer-lang-common` (`deferred-work-gate`) | `proof/rules/d03-deferred.txt` | unmarked stub in diff fails; malformed `DEFERRED(#ref)[revisit:]` fails; legacy stub passes | PENDING |
| d04 | P1 unit (T1) | `cargo test -p enforcer-core` (`run-telemetry`, re-parse every NDJSON line) | `proof/telemetry/runs.ndjson` | schema-invalid line rejected; half-line on crash -> not written | PENDING |
| d05 | P2 CI cross-platform (T1 ratchet + T2 score) | `cargo test -p enforcer-mcp` (`context-budget`) + CI `context-budget-scan` | `proof/context-budget-baseline.json` | simulated surface growth beyond tolerance fails ratchet; T2 score out of [0,1] fails | PENDING |
| d06 | P1 unit (T1) | `cargo test -p enforcer-cli` (`lifecycle-commands`) | `proof/rules/d06-lifecycle.txt` | a phase reports success while its oracle returns fail -> test fails; `review` w/o proof rows -> non-zero | PENDING |
| d07 | P1 unit (T1) | `cargo test -p enforcer-coordination` (`fix-loop`) | `proof/rules/d07-fixloop.txt` | neutral/regressing fix not reverted -> fails; loop exceeds cap -> fails; final findings > start -> fails | PENDING |
| d08 | P1 unit (T1) | `cargo test -p enforcer-mechanization` (`harness-feedback`) | `proof/rules/d08-feedback.txt` | preventable failure yields no PROPOSED row -> fails; PROPOSED gates a build -> fails | PENDING |
| d09 | P1 unit (T3 persona + T1 citation) | `cargo test -p enforcer-validator` (`doc-rule-parity`) | `proof/rules/d09-docparity.txt` | uncited or dangling-`[ruleId]` must/never bullet -> fails; persona free-text gated -> fails | PENDING |
| d10 | P1 unit (T1 obligations + T2 smells) | `cargo test -p enforcer-lang-common` (`resilience-auditor`) | `proof/rules/d10-resilience.txt` | unmet required-test row passes review -> fails; smell gates a build -> fails; score out of [0,1] fails | PENDING |
| d11 | P2 CI cross-platform (T1) | `cargo test -p enforcer-harness` (`ci-parity`) + CI `ci-parity-verify` | `proof/ci/d11-parity.txt` | injected local-only step fails; version skew fails | PENDING |
| d12 | P0 contract/schema (T1 AST) | `cargo test -p enforcer-lang-ts` (`layered-frontend`) + d01 parity | `proof/rules/d12-layered.txt` | each rule's fail-fixture must trip and pass-fixture must not; missing registry parity fails | PENDING |
| d13 | P1 unit (T1) | `cargo test -p enforcer-rules` (`rule-version-drift`) | `proof/rules/d13-drift.json` | content change w/o version bump fails; version bump w/o content change fails | PENDING |
| d14 | P0 contract/schema (T1 label over T3 content) | `cargo test -p enforcer-validator` (`ideation-skills-labeling`) | `proof/rules/d14-labeling.txt` | any `skills/ideation/**` file missing exact T3 label -> fails; skill in a rule registry -> fails | PENDING |
| d15 | doc-only (no runtime tier) | cross-link integrity check (artifact existence) | `docs/research-grounding.md` | a README claim with no numbered source; a dead cross-link -> review fails | PENDING |
| d16 | P1 unit (T1 blocks + T2 scored) | `cargo test -p enforcer-lang-common` (`fsm-*` detection tests) + d01 `rule-scaffold-parity` (5-way per ruleId) | `proof/rules/d16-fsm.txt` | raw `order.status = "shipped"` not routed through a transition -> T1 flag; FSM with no invalid-transition test -> T2 score crosses; pass triple stays clean | PENDING |
| d17 | P1 unit (T1 blocks + T2 scored) | `cargo test -p enforcer-lang-rust` (`rust-err-*` detection tests) + d01 parity | `proof/rules/d17-rust-err.txt` | `.unwrap()` in non-`#[cfg(test)]` src -> T1 flag; bare `?` w/o `.with_context` -> T2 crosses; unwrap under `#[cfg(test)]` stays clean | PENDING |
| d18 | P1 unit (T1 pattern/AST + 2 T2 audit + 1 labeled T3) | `cargo test -p enforcer-lang-security` (`security-stop-*` detection tests) + d01 parity | `proof/rules/d18-security-stop.txt` | `cursor.execute(f"...{x}")` SQLi -> T1 flag; `Md5::new()` -> T1 flag; dispatch prompt missing SECURITY-STOP Block 1 -> flag; STOP-interrupt-honoring row carries the advisory label (label presence T1) | PENDING |
| d21 | P1 unit (T1 marker grammar + T2 dep-add + T3-labeled) | `cargo test -p enforcer-lang-common` (`change-discipline`) + d01 parity | `proof/rules/d21-change-discipline.txt` | bare `// TODO: fix later` (no `(#NNNN)`) -> T1 flag; manifest gaining a dep w/o rationale -> T2 score crosses; refactor-isolation/ADR-deviation rows carry advisory label (label presence T1) | PENDING |
| d22 | P1 unit (T1 hard caps + T2 scored complexity) | `cargo test -p enforcer-lang-common` (`size-shape`) + d01 parity | `proof/rules/d22-size-shape.txt` | 201-line file / 31-line fn / 6-param sig -> T1 flag; cc=12 5-deep -> T2 crosses; trailing-pragma line >120 still flags; baselined-grown +1 line fails ratchet (composes d02) | PENDING |
| d23 | P1 unit (T1 presence + T2 quality heuristics) | `cargo test -p enforcer-lang-common` (`test-quality`) + d01 parity (consumes d16 FSM model) | `proof/rules/d23-test-quality.txt` | source file with no companion test -> T1 flag; assertion-free test -> T1 flag; `test_order_1` name / assert-on-message / FSM w/o invalid-transition test -> T2 crosses | PENDING |
| d25 | P1 unit (T1 gates + labeled T3 residue) | `cargo test -p enforcer-plan` (`orchestrator-verify-gate-{a,b,c}`) | `proof/rules/d25-orchestrator.txt` | self-reported "789 passed" with no re-run artifact -> Gate A flag; `git diff`-based staging dropping untracked source -> Gate B flag; commits-ahead-of-base unreconciled -> Gate C flag; orchestrator text in dispatch prompt -> ORCH-1.10 flag | PENDING |
| d26 | P1 unit (T1) | `cargo test -p enforcer-coordination` (`dispatch-quality-blocks`) | `proof/rules/d26-dispatch.txt` | assembled prompt missing SECURITY-STOP Block 1 -> flag; blocks out of mandated order (git boundary not dead-last) -> flag; fix-phase prompt lacking zero-match addendum -> flag; full ordered prompt stays clean | PENDING |
| d27 | P1 unit (T1/T2 mechanizable + LOOP-1.5 labeled T3) | `cargo test -p enforcer-coordination` (`loop-resilience-hooks`) + `cargo test -p enforcer-core` (`loop-resilience-meter`) | `proof/rules/d27-loop.txt` | PreCompact hook that returns non-zero on write failure -> flag; `context-meter.json` missing per-tier breakdown -> flag; hook writing when `.harness/` absent -> guard flag; `deploy.sh` installing hooks -> flag; LOOP-1.5 carries advisory label (label presence T1) | PENDING |
| d28 | P2 CI cross-platform (T1 structural + T2 DOCGATE header) | `cargo test -p enforcer-harness` (`target-ci-parity`) + d01 parity | `proof/ci/d28-target-parity.txt` | target repo with a local-only check/version vs CI -> flag; missing coverage `fail_under` -> flag; sub-project with no workflow `paths:` -> flag; missing `decisions.md` -> T1 flag; `ARCHITECTURE.md` missing required H2s -> T2 score below threshold | PENDING |

### Track E - new languages + universal scanning (e01, e-pack-dart, e-pack-cfml, e-pack-frontend-react, e-pack-python, e-pack-crypto-blockchain [OPTIONAL])

| Workpack | Proof tier(s) | Named test / oracle | Artifact path | Seeded-violation case | Status |
|----------|--------------|---------------------|---------------|-----------------------|--------|
| e01 | P1 unit (T2 scored/advisory, non-blocking) | `cargo test -p enforcer-literal-scan` (named rows `literal-scan-universal-threshold`, `literal-scan-graceful-skip`) + d01 parity | `proof/rules/e01-literal-scan.txt` | high-literal-risk source `score` must cross threshold; clean equivalent stays under; a native tool stubbed-absent -> graceful skip advisory, run still exits 0; `.dart`/`.cfc`/`.cfm` recognized by registry | PENDING |
| e-pack-dart (builds `enforcer-lang-dart`) | P0/P1 (T1 blocks + T2 scored + labeled T3) | `cargo test -p enforcer-lang-dart` (`dart-*` d01-generated detection/parity tests) + `cargo clippy`/`cargo fmt --check` clean over the NEW crate | `proof/rules/e-pack-dart.txt` | `data/` importing `presentation/` or Flutter-in-domain -> T1 flag; unchecked `!`/hardcoded API key -> T1 flag; `Color(0xFF..)` in build / `?? 0` on required field -> T2 crosses; boolean-prefix/Bloc-conventions rows carry advisory label; `dart`/`.dart` registered in the rule registry + literal-scan registry; the `enforcer-lang-dart` crate compiles + `cargo test -p` green | PENDING |
| e-pack-cfml (builds `enforcer-lang-cfml`) | P0/P1 (T1 CFLint/structural + T2 scored + labeled T3) | `cargo test -p enforcer-lang-cfml` (`cfml-*` d01-generated detection/parity tests via CFLint/CommandBox shell-out adapter) + `cargo clippy`/`cargo fmt --check` clean over the NEW crate | `proof/rules/e-pack-cfml.txt` | `queryExecute("...#rc.id#")` SQLi / `<cfquery>` in handler / `new OrderService()` (no WireBox) -> T1 flag; direct `rc` scope in service / `writeDump` diagnostics -> T2 crosses; `Utils.cfc` dumping-ground carries advisory label; `coldfusion`/`.cfc`/`.cfm` registered + adapter degrades cleanly (honest graceful-skip) when CFLint absent; the `enforcer-lang-cfml` crate compiles + `cargo test -p` green | PENDING |
| e-pack-frontend-react | P0/P1 (T1 blocks + one T2 layer-inversion) | `cargo test -p enforcer-lang-ts` (`frontend-*` d01-generated tests; named rows `frontend-family-detection`, `frontend-family-parity`) | `proof/rules/e-pack-frontend.txt` | cross-feature import / server-data-in-client-store / fetch-in-useEffect / `throw new Error` in services -> T1 flag; **`z.object` Zod usage -> FE-EFFECT-1.1 flag, `Schema.Struct` clean** (pins Effect-only divergence); components->features import -> T2 layer-inversion crosses; adds a feature `src/rules/*` file to the arc-07 skeleton | PENDING |
| e-pack-python | P0/P1 (T1 layering/DI + Python security blocks) | `cargo test -p enforcer-lang-py` (`python-fastapi-*` d01-generated detection/parity tests; named rows `python-fastapi-layered-detection`, `python-fastapi-family-parity`) | `proof/rules/e-pack-python.txt` | `routers/**` referencing a `*Repository` symbol / `Session` in `services/**` / `commit()` in a service / ORM model imported into `services/**` / `raise HTTPException` outside `routers/**` / `domain/**` importing FastAPI -> T1 flag (symbol-level, so a comment mentioning `Repository` in a pass fixture stays clean); plaintext-password store, `random.*` token, `allow_origins=["*"]` -> T1 security flag; StrEnum/enum-location consumed from d16, size/shape from d22; every `py-fastapi-*` id resolves to validator export + doc anchor + both fixtures or parity fails closed; adds a feature `src/rules/*` file to the arc-08 skeleton | PENDING |
| e-pack-crypto-blockchain (builds `enforcer-lang-crypto`) **(OPTIONAL / opt-in — OFF by default)** | P0/P1 (T1 structural + T2 heuristic + labeled T3) | `cargo test -p enforcer-lang-crypto` (named rows `crypto-family-detection`, `crypto-family-parity`, `crypto-opt-in-noop`) + `cargo clippy`/`cargo fmt --check` clean over the NEW opt-in crate | `proof/rules/e-pack-crypto-blockchain.txt` | pack OFF by default: with opt-in unset the pack no-ops (`crypto-opt-in-noop` -> zero crypto findings, run exits 0). With opt-in ON: missing Anchor account constraint / absent signer check -> T1 flag (fail `tests/fixtures/crypto/fail/missing-signer-check`, pass `tests/fixtures/crypto/pass/signer-checked`); instruction path signing a non-reconstructable message -> T1 flag via h06 (fail `fail/blind-sign`, pass `pass/reconstructable-sign`); unconstrained account -> T1 (fail `fail/unconstrained-account`, pass `pass/constrained-account`); CPI-boundary / PDA-derivation heuristics -> T2 score crosses (fail `fail/cpi-unbounded`+`fail/pda-unvalidated`, pass `pass/cpi-scoped`+`pass/pda-validated`); replay / slot-timing / sim-vs-exec / nonce-reuse -> labeled T3 for localnet proof via h07 (`label/replay-needs-localnet`, label presence T1-enforced); no product/company/game branding in rules or fixtures; the `enforcer-lang-crypto` crate compiles + `cargo test -p` green; every `crypto-*` id resolves to validator export + doc anchor + both fixtures or parity fails closed | PENDING |

### Track F - scan surface, onboarding & agent-shaping (f01 .. f05)

| Workpack | Proof tier(s) | Named test / oracle | Artifact path | Seeded-violation case | Status |
|----------|--------------|---------------------|---------------|-----------------------|--------|
| f01 | P1/P3 (T1) | `cargo test -p enforcer-scan` (`scan-modes-select`: mode enum + scope resolution over the d01 engine) | `proof/scan/f01-modes.txt` | a `full`-only violation seeded OUTSIDE the scoped path -> `scoped`/`quick` must NOT report it (scope honored), same violation INSIDE scope -> `scoped` reports it and `full` always reports it; an invalid mode string is rejected at the serde boundary (non-zero/error) | PENDING |
| f02 | P1/P5 | `cargo test -p enforcer-scan` (`onboard-scaffolds-enforce`, temp-repo fixture) | `proof/scan/f02-onboard.json` | scan a repo with no `.enforce/` -> asserts "not onboarded" error (no baseline to compare); `enforcer onboard` on a fresh repo -> `.enforce/` exists with profile + baseline + registration; a second onboard run not byte-identical / dropping waivers -> non-idempotent fail | PENDING |
| f03 | P1 (T1) | `cargo test -p enforcer-config` (`project-config-native-mode`, serde decode at boundary) | `proof/scan/f03-project-config.txt` | malformed `.enforce/config` (bad `nativeMode`) -> typed boundary parse error, no silent default; valid config -> resolver returns `augment` scoped (native+enforcer both selected); absent config -> scoped `augment` default (never whole-repo); a mode silently suppressing our checks without a gated waiver -> fails honesty | PENDING |
| f04 | P1 (T1) | `cargo test -p enforcer-core` (`run-context-agent-inline-silent`) | `proof/scan/f04-run-context.txt` | force a UI/server open under `agent-inline` -> refused (test fails if a listener binds); `human-review` -> UI/server start path reachable (loopback+token) returns structured HTML; deny-hook + MCP scan with no mode set -> resolved mode is `agent-inline` AND no server socket/UI artifact produced | PENDING |
| f05 | P1/P3 (T1) | `cargo test -p enforcer-scan` (`router-detect-route-plan` + `router-scope-narrowing` over fail/pass fixtures) + d01 parity oracle over router ids | `proof/scan/f05-route-plan.txt` | mixed `Cargo.toml`+`package.json` repo plan missing ts when package.json present -> fail (pass: rust+ts packs AND their native tools); python-only folder plan leaking rust pack -> fail (pass: python-only); crate scope resolving repo-wide -> fail (pass: single crate); unknown ext emitting a bogus T1 pack -> fail (pass: literal-scan T2-only floor, never a T1 blocker) | PENDING |

### Track G - UI layer on vendored hub dashboard/server (g01 .. g08)

All G packs honor f04 silent mode: no UI render / no server bind during inline agent runs. Each G pack proves `cargo test -p enforcer-ui` over its own disjoint `crates/enforcer-ui/src/*` sub-dir (Rust backend); the TS/web frontend is the only TS surface and its types are DERIVED from `enforcer-domain` via `ts_rs` (g05 owns the fail-closed `ts_drift` byte-compare test).

| Workpack | Proof tier(s) | Named test / oracle | Artifact path | Seeded-violation case | Status |
|----------|--------------|---------------------|---------------|-----------------------|--------|
| g01 | P5 | `cargo test -p enforcer-ui` (`serve-surface-contract`: CLI aliases resolve, loopback-default, vendored HTTP core reused) | `proof/ui/g01-serve.json` | `serve-remote-no-token` (host bind without token) -> server refuses to start; `serve-loopback-default` -> binds 127.0.0.1, returns shell HTML with the view-mount registry present; the vendored `server.js` HTTP core reimplemented instead of wrapped -> fails | PENDING |
| g02 | P3 | `cargo test -p enforcer-ui` (`report-view-contract`: violation-matrix completeness + grouping keys + silent-mode suppression) | `proof/ui/g02-report.json` | `report-silent-mode-suppressed` (f04 silent active) -> zero UI output emitted; `report-matrix-render` (fixture `.enforce/` mixed severities) -> matrix groups correctly and every row exposes `ruleId` + why/doc-anchor + file:line; a row missing anchor/location or an external asset fetched -> fails | PENDING |
| g03 | P1 (T1) | `cargo test -p enforcer-ui` (`waiver-honesty-actions`: a08 waiver shape; no silent suppression) | `proof/ui/g03-actions.json` | `ignore-without-reason-refused` (empty reason) -> action rejected, nothing written; `ignore-writes-named-waiver` -> a named `.enforce/` waiver row appears with owner+reason+ruleId (NOT a hidden mute); any code path performing a silent suppression -> fails | PENDING |
| g04 | P5 (T1) | `cargo test -p enforcer-ui` (`run-dispatch-intent`: fix-intent schema at boundary; ledger write via arc-16 `enforcer-coordination` API) | `proof/ui/g04-run-dispatch.json` | a Run payload missing ruleId/files -> rejected (schema error, zero ledger writes); a valid Run click -> one well-formed fix-intent claimable by MCP `coordination_claim`; a duplicate Run not deduping on ruleId+files (forks a lane) -> fails | PENDING |
| g05 | P1/P5 (T1) | `cargo test -p enforcer-ui` (`settings-config-writes` + `ts_drift`: writes route through c-track adapters; golden config diff; `ts_rs` committed-vs-emitted byte-compare) | `proof/ui/g05-settings.json` | a waiver save missing owner/reason/ruleId -> rejected, writes nothing; toggling a CI gate -> correct hook/CI config written once (temp-dir, diff matches golden); re-toggling ON twice not byte-identical (dup hook lines) -> fails; the UI touching config files directly instead of via adapters -> fails; committed `.ts` diverging from freshly-emitted `ts_rs` output -> `ts_drift` fails closed | PENDING |
| g06 | P3 (T1) | `cargo test -p enforcer-ui` (`hub-dashboard-mount`: live materialized state via arc-16 `enforcer-coordination`; read-only) | `proof/ui/g06-hub.json` | a corrupt/missing materialized view not rendering the empty state (throws / non-200) -> fails; a seeded ledger with one lane + one claim -> served HTML contains those exact lane/claim ids; the dashboard issuing ANY mutating call against the coordination API (spy) -> fails | PENDING |
| g07 | P5 (T1) | `cargo test -p enforcer-ui` (`ui-security-contract`: loopback-bind assertion + origin/CSRF + dispatch-authorization guards, sourced from `crates/enforcer-ui/src/security/*`) | `proof/ui/g07-security.json` | `sec-xorigin-waiver-reject` (cross-origin POST to waiver/config endpoint) -> rejected; `sec-remote-bind-no-token` (non-loopback bind without token) -> refused to start; `sec-dispatch-no-token` (dispatch without a valid intent token) -> refused; `sec-same-origin-token-ok` (same-origin + valid token to waiver/config/dispatch) -> succeeds; guards re-inlined per endpoint instead of sourced from `crates/enforcer-ui/src/security/*` -> fails | PENDING |
| g08 | P3 | `cargo test -p enforcer-ui` (`rules-skills-explorer-contract`: every rule/skill rendered from the STRUCTURED registry — meaning, fail/pass, tier, framework map) | `proof/ui/g08-explorer.json` | `explorer-renders-every-rule` (a rule in the registry with no explorer row) -> fails (completeness); `explorer-reads-structured-not-prose` (explorer sourcing a `.md` blob as the rule of record instead of the structured rule) -> fails; a row missing tier / fail-pass example / framework mapping -> fails; silent-mode active -> zero UI output | PENDING |

### Track H - money-critical & security-testing mandate (h01 .. h08, h11, h12)

Generic mechanization of the ingested money-critical / security-testing spec ([refs/security-testing-source.md](./refs/security-testing-source.md)); GENERIC across any value system, never crypto-only. All rows are scaffolded through d01 and MUST satisfy the 5-way parity for their T-tier (`ruleId <-> doc <-> validator <-> {fail-fixture + pass-fixture} <-> detection-test`) before GREEN. The h01 money-critical manifest is consumed read-only by h02/h03/h05/h06.

| Workpack | Proof tier(s) | Named test / oracle | Artifact path | Seeded-violation case | Status |
|----------|--------------|---------------------|---------------|-----------------------|--------|
| h01 | P0/P1 (T2 classifier + T1 annotation gate) | `cargo test -p enforcer-security` (`money-crit-classify` + `money-crit-annotated`) + d01 `rule-scaffold-parity` | `proof/rules/h01-money-critical.txt` | balance-crediting fn classified+unannotated -> T1 flag (fail `tests/fixtures/money-critical/classify/fail_credit_balance.ts`, pass `.../pass_credit_balance_annotated.ts`); pure formatter below threshold stays clean (`.../pass_pure_formatter.ts`); payment-signing fn unannotated -> T1 flag (fail `tests/fixtures/money-critical/annotated/fail_sign_payment_unannotated.ts`, pass `.../pass_sign_payment_registered.ts`); ambiguous value-adjacent unit defaults to money-critical unless annotated otherwise; T2 classifier score crosses threshold on value-touching code, stays under on neutral | PENDING |
| h02 | P4 (T1) | `cargo test -p enforcer-security` (`req-testcat-seven` + `req-testcat-map`) + d01 parity | `proof/rules/h02-required-test-categories.txt` | money-critical unit missing any of the seven required categories (negative + replay + concurrency + rollback/compensation + economic-exhaustion + time-based + signing/verification) -> T1 flag (fail `tests/fixtures/required-security-tests/endpoint-a/fail_missing_replay_concurrency/`, pass `.../pass_all_seven/`); a classified unit that resolves to no category-tagged test -> T1 flag (fail `.../orphan/fail_unit_no_tests/`); category detected by annotation/tag or category-named file, not heuristic guessing | PENDING |
| h03 | P1/P4 (T1) | `cargo test -p enforcer-security` (`threat-map-unit-coverage` + `threat-map-threat-has-test`) + d01 parity | `proof/rules/h03-threat-test-mapping.txt` | money-critical unit not mapping to >=1 threat + >=1 invariant + >=1 property test + >=1 concurrency test + >=1 replay test -> T1 flag (fail `tests/fixtures/threat-map/unmapped/fail_unit_absent.json`, pass `tests/fixtures/threat-map/mapped/pass_full_mapping.json`); a classified unit absent from THREAT_MAP -> T1 flag ("unmapped logic is forbidden logic"); a declared threat with zero tests -> T1 flag (fail `.../incomplete/fail_threat_zero_tests.json`, pass `.../pass_threat_with_test.json`) | PENDING |
| h04 | P1 (T1 banned/required + T2 scored) | `cargo test -p enforcer-security` (`security-test-banned-patterns`) + d01 parity | `proof/rules/h04-security-test-quality.txt` | T1 banned over security-classified tests: `mocks-for-money-logic` (fail `mock-money.fail.test.ts`, pass `real-money.pass.test.ts`), `asserts-success-only` (fail `success-only.fail.test.ts`, pass `rejects.pass.test.ts`), `non-deterministic-fuzz` w/o logged seed (fail `no-seed.fail.test.ts`, pass `seeded.pass.test.ts`), `order-dependent`/`global-mutation` (fail `shared-state.fail.test.ts`, pass `isolated.pass.test.ts`), plus pass-if-logic-deleted / rely-on-no-crash / snapshot-only; T1 required-presence: missing `asserts-rejection` or missing `reproducible-seed` flags; T2 scored: `no-threat-mapping`/`no-invariant-assertion`/`exact-failure-mode`/`fails-if-protection-removed` score crosses (fail `unmapped.fail.test.ts`, pass `mapped.pass.test.ts`) | PENDING |
| h05 | P1 (T1 presence + shape) | `cargo test -p enforcer-security` (`economic-invariant-suite`) + d01 parity | `proof/rules/h05-economic-invariants.txt` | money-critical unit missing a required invariant property -> T1 flag (fail `missing-failure-not-reward.fail/`, pass `full-suite.pass/`); an invariant "test" that is a single literal case not `fc.property` over generated inputs -> T1 non-property-shape flag (fail `idempotency-single-case.fail.test.ts`, pass `idempotency-property.pass.test.ts`); `attacker-cost >= system-cost` absent (fail `cost-no-property.fail/`, pass `cost-property.pass/`); `compensation idempotent+replay-safe` (fail `compensation-missing.fail/`, pass `compensation-property.pass/`); non-money-critical unit requires no suite (pass `plain-util.pass/`) | PENDING |
| h06 | P0/P1 mixed (T1 signing/time/boundary/killswitch + T2 economic/rollback) | `cargo test -p enforcer-security` (per-facet `{signing,time,economic,rollback,boundary,killswitch}-*` detection tests) + d01 parity | `proof/rules/h06-money-critical-mechanics.txt` | T1 SIGNING: backend signs client-raw/non-reconstructable payload -> flag (fail `sign-client-raw.fail.ts`, pass `sign-reconstructed.pass.ts`); T1 TIME: client-clock in a money path -> flag (fail `client-clock.fail.ts`, pass `server-clock.pass.ts`); T1 BOUNDARY: unauthed internal endpoint / trusted internal header -> flag (fail `unauthed-internal.fail.ts`, pass `authed-internal.pass.ts`); T1 KILL-SWITCH: not halt-all/atomic/authed/audited/replay-safe or untested -> flag (fail `killswitch-untested.fail/`+`killswitch-nonatomic.fail.ts`, pass `killswitch-full.pass/`); T2 ECONOMIC free retry with backend cost -> score crosses (fail `free-retry.fail.ts`, pass `bounded-cost.pass.ts`); T2 ROLLBACK non-idempotent (fail `rollback-nonidempotent.fail.ts`, pass `rollback-exactly-once.pass.ts`) | PENDING |
| h07 | P2/P4 (T1 coverage/sampling + T2 observability) | `cargo test -p enforcer-security` (`security-tooling-pipeline`) + d01 parity | `proof/rules/h07-security-tooling.txt` | T1: coverage below floor (line<90 / branch<80) or a drop -> CI fail (fail `fail/coverage-below-floor`, pass `pass/coverage-at-floor`); T1: security event emitted under sampling (dropped) -> fail (fail `fail/security-event-sampled`, pass `pass/security-event-unsampled`); T1: fuzz failure with no persisted seed -> fail (fail `fail/fuzz-no-seed`, pass `pass/fuzz-with-seed`); T2: money-critical path with no security log / correlation ID -> score crosses (fail `fail/money-path-no-security-log`, pass `pass/money-path-logged`); static findings stay signal-only unless threat-mapped; every tool graceful-skips with honest ran-count when the binary is absent | PENDING |
| h08 | P1 (T1 ingest-mapping + T2 unbacked-rule flag) | `cargo test -p enforcer-security` (`policy-ingest-mapping` + `profile-shape`) + d01 parity | `proof/rules/h08-testing-mandate.txt` | T1: ingesting the reference spec yields a profile whose required-test-categories + invariants equal the spec's set (pass `pass/ingest-reference-spec`); T2: an ingested spec asserting an un-backed rule is flagged for mechanization and fed to d01/d08, never silently accepted (fail `fail/ingest-unbacked-rule`); the NEUTRAL profile `profiles/money-critical-security.json` lists rule IDs + severities (T1 block / T2 score / T3 label) + required-test-categories with NO product/company/game branding anywhere in skill/profile/ingest output; the SKILL sequences the seven Track H stages (route -> h01 classify -> h02 categories -> h03 threat-map -> h05 invariants -> h06 mechanics -> h07 tooling/CI) | PENDING |
| h11 | P1/P2 (T1 predicate/frontmatter blocks + T2 scored WAF matcher) | `cargo test -p enforcer-lang-security` (`cyberskills-rule-detection` + `cyberskills-frontmatter-and-vocab` + `cyberskills-vendor-dogfood-exclusion`) + d01 `rule-scaffold-parity` (5-way per ruleId) | `proof/rules/h11-cyberskills-rust-rules.txt` | T1 IaC/cloud/manifest/header predicates: S3 block with no `server_side_encryption_configuration` / azure `enable_https_traffic_only:false` / response missing HSTS / dependency-confusion-claimable name -> flag (fail `tests/fixtures/cyberskills/iac.tf.s3-encryption-required/fail.tf`+`cloud.azure.storage-require-https/fail.json`, pass `.../pass.*`); T1 frontmatter lint: malformed `T99`/unknown MITRE-ATT&CK or NIST-CSF id not in the h03 dictionary -> flag (fail `.../skill-frontmatter/fail_bad_attack_id.md`, pass `.../pass_valid.md`); T2 `detect.waf.sqli-signature`: UNION SELECT log line `score` crosses the fail threshold, benign line stays under (fail `.../detect.waf.sqli-signature/fail.log`, pass `.../pass.log`); T1 dogfood exclusion: a self-host scan of `vendor/**` returns ZERO findings (`cyberskills-vendor-not-dogfooded`; a regression that scans the vendored Python fails) | PENDING |
| h12 | P2/P4 (T2 scored gates over tool output + T1 honest graceful-skip / dogfood-exclusion) | `cargo test -p enforcer-harness` (`cyberskills-adapter-graceful-skip` + `cyberskills-adapter-severity-gate` + `cyberskills-adapters-dogfood-exclusion`) + d01 parity | `proof/rules/h12-cyberskills-adapters.txt` | T1 graceful-skip (a09-style honest): an adapter that silently PASSES when its binary is absent -> flagged as dishonest (fail `tests/fixtures/cyberskills-adapters/slither/fail_tool_absent_reported_pass.json`, pass `.../pass_tool_absent_skipped.json` honest skip with ran-count + `.../pass_tool_present_findings.json`); a present-but-erroring tool surfaces the error, never a silent pass; T2 severity gate over RECORDED tool output: a HIGH CVE over threshold -> gate fails, below-threshold -> pass (fail `.../sca/fail_high_cve_over_threshold.json`, pass `.../sca/pass_below_threshold.json`); finding-shape parity: every adapter finding carries the fields g02 renders (ruleId/severity/location) + h03-validated ATT&CK/NIST citation where the engine supplies one; T1 dogfood exclusion: a self-host scan of `adapters/cyberskills/**` returns ZERO findings (`cyberskills-adapters-not-dogfooded`) | PENDING |

### Cross-cutting - rename + docs refresh + rename migration + branch-protection + lesson-capture + terminal dogfood gate (x01, x02, x03, x04, x05, z01)

| Workpack | Proof tier(s) | Named test / oracle | Artifact path | Seeded-violation case | Status |
|----------|--------------|---------------------|---------------|-----------------------|--------|
| x01 | P1 (T1 grep-clean + mcp:smoke) | `cargo test` (named rows `neutral-rename-grep-clean` + `neutral-rename-mcp-smoke`) | `proof/rename/x01.txt` | any residual `ocentra[-_]enforcer` token in shipped/config surfaces (`Cargo.toml` crate/bin names, renamed entry points, `enforcer.config.json`, managed-block/global-instruction) -> grep non-empty = fail; MCP stdio smoke not green under server name `enforcer` -> fail. (Scoped to shipped/config surfaces, NOT `Tools/ocentra-literal-scan/**` nor plan-doc prose.) | PENDING |
| x02 | P1 (T1 grep-clean + sections-present) | `cargo test` (`docs-refresh-check`: named rows `docs-refresh-grep-clean` + `docs-refresh-sections-present`) | `proof/docs/x02-docs-refresh.txt` | fail: a docs surface with a stale `ocentra`/`codex install` *product* reference OR a missing capability section -> gate names the offending file/section. pass: `grep -riE "ocentra\|codex install"` over owned docs product surfaces (excluding x01-owned path refs + `Tools/ocentra-literal-scan/**`) returns empty AND every new top-level capability (router f05, scan modes f01, UI g-track incl. g08 explorer, multi-harness c-track incl. c09, onboarding f02, silent-vs-human f04, dart/cfml/frontend/python) has a present non-empty doc section. | PENDING |
| x03 | P1 (T1) | `cargo test -p enforcer-install` (`rename-migration-contract`) | `proof/install/x03-rename-migration.txt` | fail (`migrate-legacy-config-present`): a harness config still carrying the old `ocentra-enforcer` server entry after migrate ran -> re-scan finds the old entry. pass (`migrate-legacy-config-rewritten`): migrate detects the legacy entry + legacy `rust_rules_*`/`ocentra_enforcer_*` tool names, rewrites the registration to `enforcer` (`mcp__enforcer__*`), emits one one-time notice, is idempotent on re-run, and a post-migration re-scan finds **zero** `ocentra-enforcer` entries (transitional, no permanent alias). | PENDING |
| x03 (legacy-skill) | P1 (T1) | `cargo test -p enforcer-install` (`legacy-skill-retired`) | `proof/install/x03-rename-migration.txt` | after migration the legacy `skills/rust-rules-hard-gate/` dir (incl. `agents/openai.yaml`) still present OR a repo/asset scan finding any `rust-rules-hard-gate` skill/descriptor residue -> fail; pass = zero `rust-rules-hard-gate` residue with the canonical `enforcer` skill remaining. | PENDING |
| x04 | P5 install-proof + P2 (T1) | `cargo test -p enforcer-install` (branch-protection verifier + emitter fixtures) | `proof/install/x04-branch-protection.txt` | fail: captured branch-protection state with no required checks / required-but-admin-bypassable+force-push-allowed / red-but-merge-eligible required check -> verifier exits non-zero, refuses to attest; pass: enforcer CI job(s) required + force-push blocked + admin override disallowed + require-up-to-date-before-merge on -> verifier exits zero, emits protection-attested verdict; emitter fixture: emitted `.github/BRANCH_PROTECTION.md` config yields the `gh api` payload producing the pass state and check contexts resolve symbolically (no hardcoded stale context). | PENDING |
| x05 | P1/P4 (T1) | `cargo test -p enforcer-plan` (`lesson-capture-loop`) | `proof/plan/x05-lessons.json` | fail: a captured non-PlanDoc lesson with ZERO landed artifacts (or an artifact missing its lesson id) -> doctor `Severity::Error` naming the lesson id, non-zero exit (not a skip/warning); a rewritten prior ledger row -> verify-on-open fails; `--dry-run` emitter touching >=1 file -> fails. pass: lesson with DoctrineBlock+Skill routes emits golden artifacts (pinned, keyed by lesson id), doctor green; seed-corpus import idempotent (re-import adds nothing). | PENDING |
| z01 | P4 self-enforce green (T1 terminal gate on plan-DONE) | `cargo test` (`dogfood-gate`; named row `dogfood-self-zero-violations`) via `xtask dogfood` — the built `enforcer` binary runs its own Rust rules on its own crates | `proof/dogfood-manifest.json` | a deliberately-planted self-violation (seeded T1 breach in a fixture repo state) makes the gate exit non-zero and refuse the DONE verdict; the clean repo state produces a PASS manifest (zero self-violations, no advisory above the committed T2 ceiling). Runs LAST, after every authoring/validating pack is DONE. | PENDING |

### Track B - planning skill (b01 .. b05)

| Workpack | Proof tier(s) | Named test / oracle | Artifact path | Seeded-violation case | Status |
|----------|--------------|---------------------|---------------|-----------------------|--------|
| b01 | P1 unit (T1) | `cargo test -p enforcer-plan` (`scaffolder-emit` + `scaffolder-determinism`) + b02 cross-check | `proof/plan/b01-emit.txt` | emitter output != golden tree; two runs differ; refuses overwrite w/o `--force` fails | PENDING |
| b02 | P4 self-enforce green (T1) | `cargo test -p enforcer-plan` (`validator-rules` + `validator-selfhost`) + PLAN-* parity, run vs THIS plan dir (`docs/plans/enforcer-selfhost-plan/`) | `proof/plan/b02-selfhost.txt` | run the PLAN-* validator against this plan dir with a seeded capsule/owns-overlap violation -> non-zero; a hollow zero-ran self-validate -> fails | PENDING |
| b02 (PLAN-RESUME-STATE) | P4 self-enforce green (T1) | `cargo test -p enforcer-plan` (`validator-rules` PLAN-RESUME-STATE row over `crates/enforcer-plan/tests/fixtures/plan-validator/**`) + `-p enforcer-rules` record parity | `proof/plan/b02-selfhost.txt` | fail fixture: a plan whose `RESUME_STATE.md`/section-set omits Where-We-Are OR any of checklist / tasklist / progress / prev / next -> the `Validator` emits a `Finding`; pass fixture: a plan carrying the complete resume-state set -> zero `Finding`s; the `ruleId` maps to a rule record + `Validator` + doc anchor + both fixtures or parity fails closed. | PENDING |
| b03 | P0 contract/schema (T1) | `cargo test -p enforcer-plan` (`templates-snapshot`) + no-inline-capsule grep/AST | `proof/plan/b03-templates.txt` | template drift vs frozen fixture; a capsule literal outside `crates/enforcer-plan/templates/` -> fails | PENDING |
| b04 | P1 unit (T1) | `cargo test -p enforcer-plan` (`orchestrator-frontier` + `orchestrator-lanes` + `orchestrator-claim-guard`, coordination fake) | `proof/plan/b04-orch.txt` | overlapping-owns concurrent claim not rejected -> fails; claim/guard/closeout out of order -> fails | PENDING |
| b05 | P4 self-enforce green (T1) | `cargo test -p enforcer-plan` (`skill-selfvalidate` + `skill-command`) + doc-parity | `proof/plan/b05-skill.txt` | `/plan` dispatches a stub not the real validator; a SKILL doctrine claim with no ruleId -> fails | PENDING |
| b06 | P1/P4 self-enforce green (T1) | `cargo test -p enforcer-plan` (`agents-forest-scaffold` + `agents-forest-chain-resolve` + `agents-forest-resume-sim` + `agents-forest-doc-intent`) over `tests/fixtures/agents_forest/{pass,fail}/**` | `proof/plan/b06-agents-forest.txt` | scaffold: rendered tier missing read-first routing marker / NEXT pointer / decision tree -> fail; chain-resolve: PASS fixtures yield zero `Finding`s, FAIL fixtures (broken NEXT pointer, missing routing block, dangling decision leaf, oversized tier) each fire their specific `ruleId` `Finding`; resume-sim: walking ONLY the `AGENTS.md` chain (global->project->plan->decision tree) resolves to the plan's current resume-state anchor without reading the plan body AND the summed chain size stays within the declared token/line budget (a broken chain fails); doc-intent: module doc + each template missing the transitional-to-typed-data statement -> fail. | PENDING |

---

## 5. Closing checklist (per workpack, before DONE)

1. Confirm the proof tier via the section-3 decision tree; if the workpack file's stated tier disagrees, resolve before proceeding.
2. Ensure the named test(s) exist and pass on the migrated tree.
3. For T1 / P4 / P5: demonstrate the seeded-violation case actually fails (a green test that never trips is a hollow scan — doctrine violation).
4. For T2: assert `score in [0,1]`, a confidence value, and zero effect on exit code.
5. For T3: assert the mechanical **label gate** passes (the prose is never trusted directly).
6. Flip this row's Status to `GREEN`, then and only then move the WORKPACK_INDEX status to DONE.
