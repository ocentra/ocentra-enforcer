apiVersion: v1
kind: Pod
metadata:
  name: web
spec:
  containers:
    - name: app
      image: app:latest
      resources:
        limits:
          cpu: "500m"
          memory: "256Mi"
        requests: {}
