[package]
name = "tree-sitter-pony-local"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false
description = """
Vendored Pony tree-sitter grammar (language-parity wave G2.3d).
Not published; local path-dependency only. See README.md for why.
"""

[lib]
path = "src/lib.rs"

[dependencies]
tree-sitter-language = "0.1"

[build-dependencies]
cc = "1"
