//! Vendored PO (gettext) tree-sitter grammar binding (language-parity wave
//! G2.5c, Tier-0). WHY VENDORED rather than a plain crates.io
//! dependency: this grammar's own published Rust binding crate pins an
//! upper-bounded pre-`tree-sitter-language` `tree-sitter` core
//! version (the same "REJECTED -- vendor instead" situation this
//! workspace's own `Cargo.toml` already documents for
//! Squirrel/Sway/Wolfram/WGSL), so it cannot be unified with this
//! workspace's own `tree-sitter = "0.25"` core dependency graph.
//! `src/parser.c`/`src/scanner.c` (where present) are unmodified
//! copies of the grammar fetched directly from the SAME upstream
//! commit the codebase-memory-mcp C baseline itself vendors (see
//! `internal/cbm/vendored/grammars/MANIFEST.md`): `tree-sitter-grammars/tree-sitter-po` @
//! `bd860a0f57f6`. MIT-licensed, matching the upstream crate's own
//! license; copyright retained verbatim in this crate's own `LICENSE`
//! file. Only this one binding file differs from any upstream Rust
//! binding -- it re-binds to the `tree-sitter-language` shim instead,
//! the same `LanguageFn`-returning shape every other grammar
//! dependency in this workspace already uses (e.g.
//! `tree-sitter-scss-local`, `tree-sitter-squirrel-local`).
//! `publish = false` in this crate's own `Cargo.toml` since it is a
//! local path-dependency only, never intended for crates.io.

use tree_sitter_language::LanguageFn;

unsafe extern "C" {
    fn tree_sitter_po() -> *const ();
}

/// Returns the PO (gettext) tree-sitter [`LanguageFn`]. Same calling
/// convention as every other grammar dependency in this workspace:
/// `let language: tree_sitter::Language = tree_sitter_po_local::language().into();`
pub fn language() -> LanguageFn {
    // SAFETY: `tree_sitter_po` is generated by `tree-sitter generate` from this
    // crate's own vendored `grammar.js` (via the upstream `tree-sitter-grammars/tree-sitter-po`
    // source this binding re-pins) and returns a `TSLanguage*`
    // matching the ABI this grammar was regenerated at -- the same
    // `LanguageFn::from_raw` contract every other grammar binding in
    // this workspace already relies on.
    unsafe { LanguageFn::from_raw(tree_sitter_po) }
}

/// The content of the vendored `node-types.json` for this grammar --
/// same convention every crates.io grammar dependency in this
/// workspace already exposes.
pub const NODE_TYPES: &str = include_str!("node-types.json");
