//! Vendored GN (Generate Ninja build-config language, `.gn`/`.gni`)
//! tree-sitter grammar binding (language-parity wave G2.5b). WHY
//! VENDORED rather than the published `tree-sitter-gn` crate directly:
//! that crate's own `Cargo.toml` pins `cc = "~1.0.83"` as a normal
//! build dependency -- confirmed via a real `cargo check` against this
//! workspace, which fails to resolve a single unified `cc` version
//! across the whole dependency graph against `tree-sitter-just`'s own
//! `cc = "^1.2.25"` requirement (Cargo unifies build-dependency version
//! requirements across the whole graph the same way it does normal
//! dependencies) -- a genuine dependency-graph incompatibility, not
//! merely an inconvenience, the same class of blocker
//! `tree-sitter-squirrel-local`'s own module doc already documents for
//! a `tree-sitter` core pin instead. This crate's own vendored
//! `src/parser.c`/`src/scanner.c` are unmodified copies of that same
//! baseline vendor directory's own generated grammar source
//! (`amaanq/tree-sitter-gn` upstream, MIT-licensed -- this crate's own
//! `LICENSE` copied verbatim from that same baseline vendor directory),
//! with only this one binding file re-pinned to
//! `tree-sitter-language = "0.1"` instead, mirroring the exact
//! `LanguageFn`-returning shape every other grammar dependency in this
//! workspace already uses. `LANGUAGE_VERSION 14` (confirmed in
//! `src/parser.c`), within this workspace's `tree-sitter = "0.25"`
//! core's 13-15 ABI range. `publish = false` in this crate's own
//! `Cargo.toml` since it is a local path-dependency only, never
//! intended for crates.io.

use tree_sitter_language::LanguageFn;

unsafe extern "C" {
    fn tree_sitter_gn() -> *const ();
}

/// Returns the GN tree-sitter [`LanguageFn`]. Same calling convention
/// as every other grammar dependency in this workspace:
/// `let language: tree_sitter::Language = tree_sitter_gn_local::language().into();`
pub fn language() -> LanguageFn {
    // SAFETY: `tree_sitter_gn` is generated by `tree-sitter generate`
    // from the upstream `amaanq/tree-sitter-gn` repo's own `grammar.js`
    // and returns a `TSLanguage*` matching the ABI 14 this grammar was
    // regenerated at -- the same `LanguageFn::from_raw` contract every
    // other grammar binding in this workspace already relies on.
    unsafe { LanguageFn::from_raw(tree_sitter_gn) }
}
