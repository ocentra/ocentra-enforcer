[package]
name = "tree-sitter-sway-local"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false
description = """
Vendored Sway tree-sitter grammar (language-parity wave G2.3e).
Not published; local path-dependency only. See src/lib.rs for why.
"""

[lib]
path = "src/lib.rs"

[dependencies]
tree-sitter-language = "0.1"

[build-dependencies]
cc = "1"
