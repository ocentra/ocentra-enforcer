[package]
name = "tree-sitter-squirrel-local"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false
description = """
Vendored Squirrel tree-sitter grammar (language-parity wave G2.2e).
Not published; local path-dependency only. See README.md for why.
"""

[lib]
path = "src/lib.rs"

[dependencies]
tree-sitter-language = "0.1"

[build-dependencies]
cc = "1"
