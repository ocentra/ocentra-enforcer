[package]
name = "enforcer-config"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Typed config load, parse-at-boundary, 3-layer resolution (embedded global profiles -> per-project override -> one resolved EffectiveConfig) for the Ocentra Enforcer workspace."
publish = false

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
# DEPENDENCY-JUSTIFICATION: typed configuration loading resolves profiles into execution context.
enforcer-core = { path = "../enforcer-core", version = "0.1.0" }
# DEPENDENCY-JUSTIFICATION: branded configuration/domain values survive profile resolution.
enforcer-domain = { path = "../enforcer-domain", version = "0.1.0" }

[lints]
workspace = true
