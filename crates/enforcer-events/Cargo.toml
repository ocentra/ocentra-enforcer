[package]
name = "enforcer-events"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Lean in-process typed event spine for the Ocentra Enforcer workspace: DomainEvent + EventEnvelope<E> with stored-decode-verifies-contract, correlation/causation IDs, and panic-isolated Sequential/Concurrent dispatch. Consumed only by scan/coordination/proof."
publish = false

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
enforcer-core = { path = "../enforcer-core", version = "0.1.0" }
enforcer-domain = { path = "../enforcer-domain", version = "0.1.0" }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
