//! Event identifier types are owned directly by `enforcer-domain`.
//!
//! This module intentionally has no compatibility re-exports: event runtime
//! code imports `enforcer_domain::events_types` at each consuming boundary.
