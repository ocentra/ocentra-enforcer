<!-- agents-forest-tier: global -->
# AGENTS.md (global, oversized)

<!-- agents-read-first -->
> READ ME FIRST. This is the GLOBAL tier of the AGENTS.md decision forest.
> Budget: stay under 40 lines / 2048 bytes for this tier.
<!-- /agents-read-first -->

<!-- agents-next-tier -->
> NEXT: pass/project/AGENTS.md
<!-- /agents-next-tier -->

<!-- agents-decision-tree -->
> DECISION TREE
> - if resuming work -> read pass/project/AGENTS.md
> LEAF -> docs/plans/enforcer-selfhost-plan/RESUME_STATE.md
<!-- /agents-decision-tree -->

<!-- agents-transitional-intent -->
> TRANSITIONAL-TO-TYPED: this file is a transitional prose surface. It is
> designed to be dropped for a typed system/db/schema later. Tauri UI later.
<!-- /agents-transitional-intent -->

Padding line 0 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 1 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 2 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 3 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 4 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 5 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 6 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 7 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 8 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 9 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 10 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 11 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 12 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 13 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 14 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 15 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 16 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 17 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 18 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 19 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 20 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 21 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 22 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 23 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 24 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 25 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 26 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 27 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 28 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
Padding line 29 to deliberately exceed the declared per-tier budget for AGENTS-BUDGET.1.
