//! Vendored Beancount tree-sitter grammar binding (language-parity
//! wave G2.5a). WHY VENDORED rather than depending on the published
//! `tree-sitter-beancount` 2.5.1 crate directly: that crate's own
//! `Cargo.toml` hard-pins `tree-sitter = "~0.26.3"` as a normal
//! (non-dev, non-optional -- its own `tree-sitter-language` Cargo
//! feature does NOT gate this dependency away, confirmed via
//! `cargo add --no-default-features --features tree-sitter-language`
//! still failing the identical resolve error) dependency, incompatible
//! with this workspace's `tree-sitter = "0.25"` core -- the same "two
//! different major-incompatible versions of the `tree-sitter` crate
//! cannot share one `tree_sitter::Language` type" conflict already
//! documented by `tree-sitter-squirrel-local`'s own module doc. This
//! crate re-pins the SAME grammar (`src/parser.c`/`scanner.c` here are
//! unmodified copies of that same published crate's own `src/`, ABI
//! 15, confirmed compatible with this workspace's `tree-sitter =
//! "0.25"` core's 13-15 range; `node-types.json` copied for
//! documentation parity too) to `tree-sitter-language = "0.1"`
//! instead. MIT-licensed, matching the upstream crate's own
//! `license = "MIT"` Cargo.toml field (the published crate ships no
//! `LICENSE` file of its own; this crate's own `LICENSE` restates the
//! standard MIT text with attribution to the upstream repository,
//! `polarmutex/tree-sitter-beancount`). `publish = false` since this
//! is a local path-dependency only.

use tree_sitter_language::LanguageFn;

unsafe extern "C" {
    fn tree_sitter_beancount() -> *const ();
}

/// Returns the Beancount tree-sitter [`LanguageFn`]. Same calling
/// convention as every other grammar dependency in this workspace:
/// `let language: tree_sitter::Language = tree_sitter_beancount_local::language().into();`
pub fn language() -> LanguageFn {
    // SAFETY: `tree_sitter_beancount` is generated by `tree-sitter
    // generate` from the upstream `tree-sitter-beancount` grammar and
    // returns a `TSLanguage*` matching the ABI 15 this grammar was
    // generated at -- the same `LanguageFn::from_raw` contract every
    // other grammar binding in this workspace already relies on.
    unsafe { LanguageFn::from_raw(tree_sitter_beancount) }
}

/// The content of the vendored `node-types.json` for this grammar --
/// same convention every crates.io grammar dependency in this
/// workspace already exposes.
pub const NODE_TYPES: &str = include_str!("node-types.json");
