[package]
name = "_workspace_placeholder"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false
description = "Placeholder workspace member so `members = [\"crates/*\"]` resolves before arc-01 adds enforcer-core. Delete this crate directory in arc-01."

[lints]
workspace = true
