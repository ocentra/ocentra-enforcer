//! Placeholder crate. Exists only so the root `Cargo.toml` workspace glob
//! `members = ["crates/*"]` resolves to at least one member before arc-01
//! adds the first real crate (`enforcer-core`). Remove this directory when
//! arc-01 lands.
