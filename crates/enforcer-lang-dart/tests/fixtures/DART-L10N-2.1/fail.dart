// FAIL fixture for DART-L10N-2.1: hardcoded user-facing string (scored).
class SubmitButton extends StatelessWidget {
  Widget build(BuildContext context) {
    Text('Submit order');
    return const Placeholder();
  }
}
