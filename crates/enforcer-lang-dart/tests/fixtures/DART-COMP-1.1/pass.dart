// PASS fixture for DART-COMP-1.1: a single public widget in this file.
class OrderCard extends StatelessWidget {
  const OrderCard({super.key});

  Widget build(BuildContext context) => const Placeholder();
}

class _OrderCardBadge extends StatelessWidget {
  const _OrderCardBadge({super.key});

  Widget build(BuildContext context) => const Placeholder();
}
