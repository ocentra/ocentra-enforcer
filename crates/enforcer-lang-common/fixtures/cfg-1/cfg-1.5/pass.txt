// Fixture for CFG-1.5: Public re-export allow mode is forbidden in strict profiles
// Clean source — no occurrence of the CFG-1.5 marker below.
fn clean_example() {
    // conformant with common/config-lockdown
}
