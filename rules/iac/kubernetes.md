# Kubernetes Rules

Static Kubernetes checks operate on manifest files ending in `.k8s.yaml` or `.k8s.yml`.

# Covered Rules

- `IAC-1.8`: Container security contexts must not set `privileged: true`.
