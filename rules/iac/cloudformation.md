# CloudFormation Rules

Static CloudFormation checks operate on JSON and YAML template source.

# Covered Rules

- `IAC-1.4`: S3 buckets must configure a complete public-access block.
- `IAC-1.5`: IAM statements must not combine wildcard action and wildcard resource.
